// Generated by KAIN Compiler (Project Ouroboros)
// Do not edit - regenerate from .kn source

#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_parens)]

use std::collections::HashMap;
use std::rc::Rc;
use std::cell::RefCell;
use std::cmp::min;
use std::ffi::c_void;
use std::mem::size_of;

unsafe extern "C" {
    fn malloc(size: usize) -> *mut c_void;
    fn calloc(count: usize, size: usize) -> *mut c_void;
    fn realloc(ptr: *mut c_void, size: usize) -> *mut c_void;
}
fn __kain_pow(left: f64, right: f64) -> f64 {
    left.powf(right)
}
fn __kain_alloc_bytes(size: usize, zeroed: bool) -> *mut u8 {
    unsafe {
        if zeroed { calloc(1, size) as *mut u8 } else { malloc(size) as *mut u8 }
    }
}
fn __kain_realloc_bytes(ptr: *mut u8, size: usize, _zeroed_new: bool) -> *mut u8 {
    unsafe { realloc(ptr.cast::<c_void>(), size) as *mut u8 }
}

fn __kain_union_wrap<TObject: Copy, TValue: Copy>(mut value: TObject, _active: &str, _type_key: &str, byte_size: i64, union_size: i64, active_value: TValue) -> TObject {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&active_value as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    value
}
fn __kain_union_get<TObject: Copy, TValue: Copy>(value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, fallback: TValue) -> TValue {
    let mut result = fallback;
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe { if copy_span > 0 { std::ptr::copy_nonoverlapping((&value as *const TObject).cast::<u8>(), (&mut result as *mut TValue).cast::<u8>(), copy_span); } }
    result
}
fn __kain_union_set<TObject: Copy, TValue: Copy>(mut value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, next: TValue) -> TValue {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&next as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    next
}
fn __kain_load_bitfield_unit<T: Copy>(value: &T, unit_offset: i64) -> u64 {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return 0; }
    let mut unit = 0u64;
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((value as *const T).cast::<u8>().add(unit_offset as usize), (&mut unit as *mut u64).cast::<u8>(), available); }
    unit
}
fn __kain_store_bitfield_unit<T: Copy>(value: &mut T, unit_offset: i64, unit: u64) {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return; }
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((&unit as *const u64).cast::<u8>(), (value as *mut T).cast::<u8>().add(unit_offset as usize), available); }
}
fn __kain_bitfield_mask(width: i64) -> u64 { if width <= 0 { 0 } else if width >= 64 { u64::MAX } else { (1u64 << width) - 1 } }
fn __kain_sign_extend(value: u64, width: i64) -> i64 { if width <= 0 { 0 } else if width >= 64 { value as i64 } else { let sign_bit = 1u64 << (width - 1); if (value & sign_bit) == 0 { value as i64 } else { (value | !__kain_bitfield_mask(width)) as i64 } } }
fn __kain_bitfield_get<T: Copy>(value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, _promoted_bits: i64) -> i64 {
    let mask = __kain_bitfield_mask(width);
    let unit = __kain_load_bitfield_unit(&value, unit_offset);
    let shifted = if bit_offset <= 0 { unit } else { unit >> bit_offset };
    let encoded = shifted & mask;
    if is_signed { __kain_sign_extend(encoded, width) } else { encoded as i64 }
}
fn __kain_bitfield_set<T: Copy, TValue: Copy + Into<i64>>(mut value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, promoted_bits: i64, next: TValue) -> TValue {
    let mask = __kain_bitfield_mask(width);
    let mut unit = __kain_load_bitfield_unit(&value, unit_offset);
    let encoded = (next.into() as u64) & mask;
    let shifted_mask = if bit_offset <= 0 { mask } else { mask << bit_offset };
    unit = (unit & !shifted_mask) | if bit_offset <= 0 { encoded } else { encoded << bit_offset };
    __kain_store_bitfield_unit(&mut value, unit_offset, unit);
    let _ = __kain_bitfield_get(value, "", unit_offset, bit_offset, width, is_signed, promoted_bits);
    next
}

#[derive(Debug, Clone, Default)]
struct KClonerProps {
    pub arg_0: Any,
    pub children: String,
}

fn KCloner(props: KClonerProps) -> String {
    let KClonerProps { arg_0, children } = props;

    let mut isPlaying: Any = true;
    let mut fps: Any = 30;
    let mut baking: Any = false;
    let mut status: Any = "kipp engine 0.6 beta".to_string();
    let mut exportFormat: Any = "GLB".to_string();
    let mut exportMode: Any = "ANIM".to_string();
    let mut targetEngine: Any = "GENERIC".to_string();
    let mut splitAnimations: Any = false;
    let mut normalizeScale: Any = true;
    let mut distributionMode: Any = "ROUND_ROBIN".to_string();
    let mut keyframes: Record<String, [Any]> = Any();
    let mut activeKeyId: Option<String> = None;
    let mut timeRef: Any = useRef(0);
    let mut chains: Any = || { let initialChain = createChainInstance(0); (initialChain.modifiers = vec![createModifierInstance("FLOAT".to_string())]); return vec![initialChain]; };
    let mut activeChainId: Any = || chains[0].id;
    let mut playbackChainId: Option<String> = None;
    let mut selectedModId: Option<String> = || chains[0].modifiers[0].instanceId;
    let mut clonerMode: Any = "GRID".to_string();
    let mut clonerCount: Any = Any();
    let mut clonerSpacing: Any = Any();
    let mut radialRadius: Any = 5;
    let mut radialCount: Any = 8;
    let mut mountRef: Any = useRef(None);
    let mut fileInputRef: Any = useRef(None);
    let mut stageRef: Any = useRef(None);
    let mut clockRef: Any = useRef(THREE.Clock());
    let mut lastBlobRef: Any = useRef(None);
    let mut meshGroupRef: Any = useRef(None);
    let mut dummyRef: Any = useRef(THREE.Object3D());
    let mut originalsRef: Any = useRef(vec![]);
    let mut geometriesRef: Any = useRef(vec![]);
    let mut materialsRef: Any = useRef(vec![]);
    let mut scriptCache: Any = useRef(Any());
    let mut totalDuration: Any = chains.reduce(|acc: Any, chain: Any| ((acc + chain.duration)), 0);
    let mut stateRef: Any = useRef(Any());
    let mut sequencerTracks: [SequencerTrack] = Object.keys(keyframes).map(|id: Any| (Any()));
    let mut prevModeRef: Any = useRef(clonerMode);
    let mut uiTime: Any = 0;

    let mut setIsPlaying = |value: Any| {
        isPlaying = value;
    };

    let mut setFps = |value: Any| {
        fps = value;
    };

    let mut setBaking = |value: Any| {
        baking = value;
    };

    let mut setStatus = |value: Any| {
        status = value;
    };

    let mut setExportFormat = |value: Any| {
        exportFormat = value;
    };

    let mut setExportMode = |value: Any| {
        exportMode = value;
    };

    let mut setTargetEngine = |value: Any| {
        targetEngine = value;
    };

    let mut setSplitAnimations = |value: Any| {
        splitAnimations = value;
    };

    let mut setNormalizeScale = |value: Any| {
        normalizeScale = value;
    };

    let mut setDistributionMode = |value: Any| {
        distributionMode = value;
    };

    let mut setKeyframes = |value: Record<String, [Any]>| {
        keyframes = value;
    };

    let mut setActiveKeyId = |value: Option<String>| {
        activeKeyId = value;
    };

    let mut setChains = |value: Any| {
        chains = value;
    };

    let mut setActiveChainId = |value: Any| {
        activeChainId = value;
    };

    let mut setPlaybackChainId = |value: Option<String>| {
        playbackChainId = value;
    };

    let mut setSelectedModId = |value: Option<String>| {
        selectedModId = value;
    };

    let mut setClonerMode = |value: Any| {
        clonerMode = value;
    };

    let mut setClonerCount = |value: Any| {
        clonerCount = value;
    };

    let mut setClonerSpacing = |value: Any| {
        clonerSpacing = value;
    };

    let mut setRadialRadius = |value: Any| {
        radialRadius = value;
    };

    let mut setRadialCount = |value: Any| {
        radialCount = value;
    };

    let mut getVal = |id: String, def: f64, timeOverride: f64| {
        let t = ((timeOverride || timeRef.current));
        let track = keyframes[id];
        if ((((!track)) || ((track.length == 0)))) { return def; };
        let sorted = vec![track].sort(|a: Any, b: Any| ((a.t - b.t)));
        if ((t <= sorted[0].t)) { return sorted[0].v; };
        if ((t >= sorted[((sorted.length - 1))].t)) { return sorted[((sorted.length - 1))].v; };
        let idx = sorted.findIndex(|k: Any| ((k.t > t)));
        if ((idx == ((-1)))) { return sorted[((sorted.length - 1))].v; };
        let k1 = sorted[((idx - 1))];
        let k2 = sorted[idx];
        let pct = (((((t - k1.t))) / (((k2.t - k1.t)))));
        return ((k1.v + (((((k2.v - k1.v))) * pct))));
    };

    let mut toggleKey = |id: String, val: f64| {
        let t = parseFloat(timeRef.current.toFixed(2));
        setKeyframes(|prev: Any| { let track = ((prev[id] || vec![])); let existsIdx = track.findIndex(|k: Any| ((Math.abs(((k.t - t))) < 0.1))); if ((existsIdx >= 0)) { let next = vec![track]; next.splice(existsIdx, 1); let res = Any(); if ((next.length == 0)) { res[id] }; return res; } else { return Any(); } });
    };

    let mut rebuildLayout = || {
        let group = meshGroupRef.current;
        if ((!group)) { return; };
        let _ = stateRef.current;
        let origins: [Any] = vec![];
        let totalCount = 0;
        if ((clonerMode == "SINGLE".to_string())) { origins.push(Any()); (totalCount = 1) } else if ((clonerMode == "GRID".to_string())) { let offX = (((((((clonerCount.x - 1))) * clonerSpacing.x)) * 0.5)); let offY = (((((((((clonerCount.y - 1))) * clonerSpacing.y)) - (((((((clonerCount.y - 1))) * clonerSpacing.y)) * 0.5)))) + 0.5)); let offZ = (((((((clonerCount.z - 1))) * clonerSpacing.z)) * 0.5)); let x = 0; while ((x < clonerCount.x)) { let y = 0; while ((y < clonerCount.y)) { let z = 0; while ((z < clonerCount.z)) { origins.push(Any()); (totalCount = ((totalCount + 1))); (z = ((z + 1))) } (y = ((y + 1))) } (x = ((x + 1))) } } else if ((clonerMode == "RADIAL".to_string())) { let step = (((((Math.PI * 2))) / radialCount)); let i = 0; while ((i < radialCount)) { let angle = ((step * i)); origins.push(Any()); (totalCount = ((totalCount + 1))); (i = ((i + 1))) } } else if ((clonerMode == "LINEAR".to_string())) { let offY = (((((((clonerCount.y - 1))) * clonerSpacing.y)) * 0.5)); let i = 0; while ((i < clonerCount.y)) { origins.push(Any()); (totalCount = ((totalCount + 1))); (i = ((i + 1))) } };
        originalsRef.current = origins;
        let geometries = geometriesRef.current;
        let materials = materialsRef.current;
        let numVariants = geometries.length;
        if ((numVariants == 0)) { return; };
        while ((group.children.length > numVariants)) {
            let mesh = group.children.pop();
            if mesh { mesh.dispose() };
        }
        while ((group.children.length < numVariants)) {
            let mesh = THREE.InstancedMesh(geometries[group.children.length], materials[group.children.length], 10000);
            mesh.castShadow = true;
            mesh.receiveShadow = true;
            mesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
            mesh.userData.globalIndices = vec![];
            group.add(mesh);
        }
        let countsPerVariant = Array(numVariants).fill(0);
        let assignments = Int32Array(totalCount);
        let i = 0;
        while ((i < totalCount)) {
            assignments[i] = if ((distributionMode == "RANDOM".to_string())) { Math.floor(((Math.random() * numVariants))) } else { ((i % numVariants)) };
            i = ((i + 1));
        }
        let i = 0;
        while ((i < totalCount)) {
            countsPerVariant[assignments[i]] = ((countsPerVariant[assignments[i]] + 1));
            i = ((i + 1));
        }
        let iter_1 = group.children;
        let index_2 = 0;
        while ((index_2 < iter_1.length)) {
            let mesh = iter_1[index_2];
            let vIdx = index_2;
            let needed = countsPerVariant[vIdx];
            if ((needed > mesh.instanceMatrix.count)) { let newMesh = THREE.InstancedMesh(geometries[vIdx], materials[vIdx], Math.max(((mesh.instanceMatrix.count * 2)), ((needed + 1000)))); (newMesh.castShadow = true); (newMesh.receiveShadow = true); newMesh.instanceMatrix.setUsage(THREE.DynamicDrawUsage); (group.children[vIdx] = newMesh); mesh.dispose(); (mesh = newMesh) };
            mesh.count = needed;
            mesh.userData.globalIndices = Int32Array(needed);
            mesh.userData.localCounter = 0;
            mesh.geometry = geometries[vIdx];
            mesh.material = materials[vIdx];
            index_2 = ((index_2 + 1));
        }
        let dum = dummyRef.current;
        let i = 0;
        while ((i < totalCount)) {
            let variantIdx = assignments[i];
            let mesh = group.children[variantIdx];
            let localIdx = (mesh.userData.localCounter = ((mesh.userData.localCounter + 1)));
            mesh.userData.globalIndices[localIdx] = i;
            let o = origins[i];
            dum.position.copy(o.pos);
            dum.rotation.copy(o.rot);
            dum.scale.copy(o.scale);
            dum.updateMatrix();
            mesh.setMatrixAt(localIdx, dum.matrix_);
            i = ((i + 1));
        }
        let iter_3 = group.children;
        let index_4 = 0;
        while ((index_4 < iter_3.length)) {
            let m = iter_3[index_4];
            m.instanceMatrix.needsUpdate = true;
            m.userData.localCounter;
            index_4 = ((index_4 + 1));
        }
    };

    let mut applyModifierLogic = |obj: Any, mod_: Any, t: f64, index: f64, getK: Any| {
        let baseP = mod_.params;
        let getP = |k: String| getK(((("mod_".to_string() + mod_.instanceId) + "_".to_string()) + k), baseP[k]);
        let p: Any = Proxy(baseP, Any());
        let step = ((baseP.step || 0.1));
        let time = ((t + ((index * step))));
        let DEG = ((Math.PI / 180));
        if ((mod_.type_ == "orbit".to_string())) { let rr = ((((((time * p.speed)) * Math.PI)) * 2)); if ((p.axis == "y".to_string())) { (obj.rotation.y = ((obj.rotation.y + rr))) } else if ((p.axis == "x".to_string())) { (obj.rotation.x = ((obj.rotation.x + rr))) } else { (obj.rotation.z = ((obj.rotation.z + rr))) }; break; } else if ((mod_.type_ == "float".to_string())) { (obj.position.y = ((obj.position.y + ((Math.sin(((((time * p.speed)) + (((p.phase || 0)))))) * p.height))))) } else if ((mod_.type_ == "pulse".to_string())) { obj.scale.multiplyScalar(((1 + ((Math.sin(((time * p.speed))) * p.scale))))) } else if ((mod_.type_ == "shake".to_string())) { (obj.position.x = ((obj.position.x + (((((noise(((((t * p.frequency)) + index))) - 0.5))) * p.intensity))))); (obj.position.y = ((obj.position.y + (((((noise(((((((t * p.frequency)) + 100)) + index))) - 0.5))) * p.intensity))))); (obj.position.z = ((obj.position.z + (((((noise(((((((t * p.frequency)) + 200)) + index))) - 0.5))) * p.intensity))))) } else if ((mod_.type_ == "elastic".to_string())) { let stretch = ((Math.sin(((((time * p.speed)) + ((index * step))))) * p.amount)); if ((p.axis == "y".to_string())) { (obj.scale.y = ((obj.scale.y + stretch))); (obj.scale.x = ((obj.scale.x - ((stretch * 0.5))))); (obj.scale.z = ((obj.scale.z - ((stretch * 0.5))))) } else if ((p.axis == "x".to_string())) { (obj.scale.x = ((obj.scale.x + stretch))); (obj.scale.y = ((obj.scale.y - ((stretch * 0.5))))); (obj.scale.z = ((obj.scale.z - ((stretch * 0.5))))) } else { (obj.scale.z = ((obj.scale.z + stretch))); (obj.scale.x = ((obj.scale.x - ((stretch * 0.5))))); (obj.scale.y = ((obj.scale.y - ((stretch * 0.5))))) }; break; } else if ((mod_.type_ == "pendulum".to_string())) { let swing = ((((Math.sin(((time * p.speed))) * p.angle)) * DEG)); if ((p.axis == "z".to_string())) { (obj.rotation.z = ((obj.rotation.z + swing))) } else if ((p.axis == "x".to_string())) { (obj.rotation.x = ((obj.rotation.x + swing))) } else { (obj.rotation.y = ((obj.rotation.y + swing))) }; break; } else if ((mod_.type_ == "wobble".to_string())) { (obj.rotation.x = ((obj.rotation.x + ((Math.sin(((((((time * p.speed)) * 1.1)) + index))) * p.intensity))))); (obj.rotation.z = ((obj.rotation.z + ((Math.cos(((((((time * p.speed)) * 0.9)) + index))) * p.intensity))))); break; } else if ((mod_.type_ == "figure8".to_string())) { let a = ((time * p.speed)); (obj.position.x = ((obj.position.x + ((Math.sin(a) * p.width))))); (obj.position.y = ((obj.position.y + ((Math.sin(((a * 2))) * p.height))))); break; } else if ((mod_.type_ == "heartbeat".to_string())) { let period = ((60 / p.bpm)); let phase = (((((t % period))) / period)); let beat = if ((phase < 0.1)) { Math.sin(((((phase / 0.1)) * Math.PI))) } else { if ((phase < 0.2)) { ((Math.sin((((((((phase - 0.1))) / 0.1)) * Math.PI))) * 0.5)) } else { 0 } }; obj.scale.multiplyScalar(((1 + ((beat * p.intensity))))); break; } else if ((mod_.type_ == "glitch".to_string())) { let slot = Math.floor(((t / p.interval))); let rng = hash(((((slot * 1234)) + ((index * 5678))))); if ((rng > 0.7)) { (obj.position.x = ((obj.position.x + (((((((hash(((slot + 1))) - 0.5))) * p.scatter)) * 2))))); (obj.position.y = ((obj.position.y + (((((((hash(((slot + 2))) - 0.5))) * p.scatter)) * 2))))); (obj.position.z = ((obj.position.z + (((((((hash(((slot + 3))) - 0.5))) * p.scatter)) * 2))))) }; break; } else if ((mod_.type_ == "step".to_string())) { let stepT = ((Math.floor(((t * p.fps))) / p.fps)); if ((((!obj.userData.stepLast)) || ((Math.abs(((obj.userData.stepLast - stepT))) > 0.0)))) { (obj.userData.stepLast = stepT); (obj.userData.stepPos = obj.position.clone()); (obj.userData.stepRot = obj.rotation.clone()) }; if obj.userData.stepPos { obj.position.copy(obj.userData.stepPos); obj.rotation.copy(obj.userData.stepRot) }; break; } else if ((mod_.type_ == "bounce".to_string())) { let bt = (((((time * p.speed))) % (Math.PI))); let h = ((Math.abs(Math.sin(bt)) * p.height)); (obj.position.y = ((obj.position.y + h))); let squash = ((1 - Math.max(0, (((((1 - ((h / p.height))))) * p.squash))))); (obj.scale.y = ((obj.scale.y * squash))); (obj.scale.x = ((obj.scale.x / Math.sqrt(squash)))); (obj.scale.z = ((obj.scale.z / Math.sqrt(squash)))); break; } else if ((mod_.type_ == "tumble".to_string())) { (obj.rotation.x = ((obj.rotation.x + ((time * p.speedX))))); (obj.rotation.y = ((obj.rotation.y + ((time * p.speedY))))); (obj.rotation.z = ((obj.rotation.z + ((time * p.speedZ))))) } else if ((mod_.type_ == "strobe".to_string())) { let cycle = (((((t * p.speed))) % 1)); if ((cycle > p.duty)) { obj.scale.set(0.0, 0.0, 0.0) }; break; } else if ((mod_.type_ == "corkscrew".to_string())) { let angle = ((((((((time * p.speed)) * Math.PI)) * 2)) * p.rotations)); (obj.position.x = ((obj.position.x + ((Math.cos(angle) * 0.3))))); (obj.position.y = ((obj.position.y + ((((time * p.height)) * p.speed))))); (obj.position.z = ((obj.position.z + ((Math.sin(angle) * 0.3))))); (obj.rotation.y = ((obj.rotation.y + ((((time * p.speed)) * p.rotations))))); break; } else if ((mod_.type_ == "shiver".to_string())) { (obj.position.x = ((obj.position.x + (((((Math.random() - 0.5))) * p.intensity))))); (obj.position.y = ((obj.position.y + (((((Math.random() - 0.5))) * p.intensity))))); (obj.position.z = ((obj.position.z + (((((Math.random() - 0.5))) * p.intensity))))) } else if ((mod_.type_ == "sway".to_string())) { let swayAngle = ((((Math.sin(((((time * p.speed)) + ((index * step))))) * p.angle)) * DEG)); (obj.rotation.z = ((obj.rotation.z + swayAngle))); break; } else if ((mod_.type_ == "yoyo".to_string())) { let yo = Math.abs(Math.sin(((time * p.speed)))); (obj.position.y = ((obj.position.y + ((yo * p.length))))); break; } else if ((mod_.type_ == "crab".to_string())) { (obj.position.x = ((obj.position.x + ((Math.sin(((((time * p.speed)) + ((index * step))))) * p.width))))); break; } else if ((mod_.type_ == "lissajous".to_string())) { let lx = ((Math.sin(((((((time * p.speed)) * p.a)) + ((Math.PI / 2))))) * p.size)); let ly = ((((Math.sin(((((time * p.speed)) * p.b))) * p.size)) * 0.5)); (obj.position.x = ((obj.position.x + lx))); (obj.position.y = ((obj.position.y + ly))); break; } else if ((mod_.type_ == "flip".to_string())) { let flipT = (((((Math.floor(((t / p.interval))) + index))) % 2)); let progress = Math.min(1, ((((((t % p.interval))) * p.speed)))); let angle = ((((flipT * Math.PI)) + ((progress * Math.PI)))); if ((p.axis == "x".to_string())) { (obj.rotation.x = ((obj.rotation.x + angle))) } else if ((p.axis == "z".to_string())) { (obj.rotation.z = ((obj.rotation.z + angle))) } else { (obj.rotation.y = ((obj.rotation.y + angle))) }; break; } else if ((mod_.type_ == "tremor".to_string())) { (obj.position.x = ((obj.position.x + ((Math.sin(((((t * p.speed)) + ((index * 37.3))))) * p.intensity))))); (obj.position.z = ((obj.position.z + ((Math.cos(((((t * p.speed)) + ((index * 19.7))))) * p.intensity))))) } else if ((mod_.type_ == "scan".to_string())) { let scanT = Math.sin(((time * p.speed))); if ((p.axis == "x".to_string())) { (obj.position.x = ((obj.position.x + ((scanT * p.distance))))) } else if ((p.axis == "z".to_string())) { (obj.position.z = ((obj.position.z + ((scanT * p.distance))))) } else { (obj.position.y = ((obj.position.y + ((scanT * p.distance))))) }; break; } else if ((mod_.type_ == "warp".to_string())) { let wt = ((time * p.speed)); let sx = ((1 + ((Math.sin(((wt + index))) * p.stretch)))); let sy = ((1 + ((Math.cos(((((wt * 1.3)) + index))) * p.stretch)))); (obj.scale.x = ((obj.scale.x * sx))); (obj.scale.y = ((obj.scale.y * sy))); break; } else if ((mod_.type_ == "drift".to_string())) { let da = ((time * p.speed)); (obj.position.x = ((obj.position.x + ((((Math.cos(((da + ((index * 1.2))))) * p.radius)) * 0.0))))); (obj.position.z = ((obj.position.z + ((((Math.sin(((da + ((index * 1.2))))) * p.radius)) * 0.0))))); break; } else if ((mod_.type_ == "bobble".to_string())) { (obj.position.y = ((obj.position.y + ((Math.sin(((((time * p.speed)) + ((index * step))))) * p.amount))))); (obj.rotation.z = ((obj.rotation.z + ((Math.cos(((((((time * p.speed)) * 1.1)) + index))) * 0.1))))); break; } else if ((mod_.type_ == "twist".to_string())) { let twistAngle = ((((Math.sin(((((time * p.speed)) + ((index * step))))) * p.angle)) * DEG)); if ((p.axis == "y".to_string())) { (obj.rotation.y = ((obj.rotation.y + twistAngle))) } else if ((p.axis == "x".to_string())) { (obj.rotation.x = ((obj.rotation.x + twistAngle))) } else { (obj.rotation.z = ((obj.rotation.z + twistAngle))) }; break; } else if ((mod_.type_ == "spiral".to_string())) { let sa = ((((time * p.speed)) + ((index * (((((Math.PI * 2)) / Math.max(1, p.rotations)))))))); let sr = ((p.radius * (((1 + ((((index * p.grow)) * 0.0))))))); (obj.position.x = ((obj.position.x + ((((Math.cos(sa) * sr)) * 0.0))))); (obj.position.z = ((obj.position.z + ((((Math.sin(sa) * sr)) * 0.0))))); (obj.position.y = ((obj.position.y + ((((time * p.speed)) * 0.0))))); break; } else if ((mod_.type_ == "vortex".to_string())) { let dist = ((Math.sqrt((((__kain_pow((obj.position.x) as f64, (2) as f64)) + (__kain_pow((obj.position.z) as f64, (2) as f64))))) || 0.0)); let pull = ((p.strength / (((dist + p.falloff))))); let vAngle = ((time * p.speed)); (obj.position.x = ((obj.position.x + (((((((((-Math.sin(vAngle))) * pull)) + (((((0 - obj.position.x))) * 0.0))))) * Math.min(((dist / p.radius)), 1)))))); (obj.position.z = ((obj.position.z + (((((((Math.cos(vAngle) * pull)) + (((((0 - obj.position.z))) * 0.0))))) * Math.min(((dist / p.radius)), 1)))))); break; } else if ((mod_.type_ == "magnet".to_string())) { let mx = obj.position.x; let mz = obj.position.z; let md = ((Math.sqrt(((((mx * mx)) + ((mz * mz))))) + 0.0)); if ((md < p.range)) { let force = ((((((p.strength * (((1 - ((md / p.range))))))) * p.speed)) * 0.0)); (obj.position.x = ((obj.position.x - (((((mx / md))) * force))))); (obj.position.z = ((obj.position.z - (((((mz / md))) * force))))) }; break; } else if ((mod_.type_ == "noise_flow".to_string())) { let ns = p.scale; (obj.position.x = ((obj.position.x + (((((((noise(((((obj.position.x * ns)) + ((t * p.speed))))) - 0.5))) * p.force)) * 0.1))))); (obj.position.y = ((obj.position.y + (((((((noise(((((((obj.position.y * ns)) + ((t * p.speed)))) + 100))) - 0.5))) * p.force)) * 0.0))))); (obj.position.z = ((obj.position.z + (((((((noise(((((((obj.position.z * ns)) + ((t * p.speed)))) + 200))) - 0.5))) * p.force)) * 0.1))))); break; } else if ((mod_.type_ == "ripple".to_string())) { let rDist = Math.sqrt((((__kain_pow((obj.position.x) as f64, (2) as f64)) + (__kain_pow((obj.position.z) as f64, (2) as f64))))); let rWave = ((((Math.sin(((((rDist * p.frequency)) - ((time * p.speed))))) * p.amplitude)) * Math.exp(((((-rDist)) * p.decay))))); (obj.position.y = ((obj.position.y + rWave))); break; } else if ((mod_.type_ == "squash".to_string())) { let sq = ((1 + ((Math.sin(((((time * p.speed)) + ((index * step))))) * p.amount)))); if ((p.axis == "y".to_string())) { (obj.scale.y = ((obj.scale.y * sq))); (obj.scale.x = ((obj.scale.x * ((1 / Math.sqrt(sq)))))); (obj.scale.z = ((obj.scale.z * ((1 / Math.sqrt(sq)))))) } else if ((p.axis == "x".to_string())) { (obj.scale.x = ((obj.scale.x * sq))); (obj.scale.y = ((obj.scale.y * ((1 / Math.sqrt(sq)))))); (obj.scale.z = ((obj.scale.z * ((1 / Math.sqrt(sq)))))) } else { (obj.scale.z = ((obj.scale.z * sq))); (obj.scale.x = ((obj.scale.x * ((1 / Math.sqrt(sq)))))); (obj.scale.y = ((obj.scale.y * ((1 / Math.sqrt(sq)))))) }; break; } else if ((mod_.type_ == "accordion".to_string())) { let acc = ((Math.sin(((((time * p.speed)) + ((index * step))))) * p.amount)); if ((p.axis == "y".to_string())) { (obj.scale.y = ((obj.scale.y + acc))) } else if ((p.axis == "x".to_string())) { (obj.scale.x = ((obj.scale.x + acc))) } else { (obj.scale.z = ((obj.scale.z + acc))) }; break; } else if ((mod_.type_ == "chaos".to_string())) { let ct = ((((t * p.speed)) + index)); (obj.position.x = ((obj.position.x + (((((((noise(ct) - 0.5))) * p.scale)) * 0.1))))); (obj.position.y = ((obj.position.y + (((((((noise(((ct + 33))) - 0.5))) * p.scale)) * 0.1))))); (obj.position.z = ((obj.position.z + (((((((noise(((ct + 66))) - 0.5))) * p.scale)) * 0.1))))); (obj.rotation.y = ((obj.rotation.y + (((((noise(((ct + 99))) - 0.5))) * 0.1))))); break; } else if ((mod_.type_ == "breathe".to_string())) { let breath = ((1 + ((Math.sin(((((((((time * p.speed)) * Math.PI)) * 2)) + ((index * step))))) * p.amount)))); obj.scale.multiplyScalar(breath); break; } else if ((mod_.type_ == "explode".to_string())) { if ((t > p.trigger)) { let age = ((t - p.trigger)); let dir = THREE.Vector3(((hash(((((index * 3)) + 1))) - 0.5)), hash(((((index * 3)) + 2))), ((hash(((((index * 3)) + 3))) - 0.5))).normalize(); let strength = ((p.strength * Math.exp(((((-age)) * p.decay))))); obj.position.addScaledVector(dir, ((strength * 0.0))); (obj.rotation.x = ((obj.rotation.x + ((dir.x * 0.1))))); (obj.rotation.z = ((obj.rotation.z + ((dir.z * 0.1))))) }; break; } else if ((mod_.type_ == "code".to_string())) { let cacheKey = mod_.instanceId; let script = scriptCache.current[cacheKey]; let v: Any = Any(); if p.sliders { let iter_5 = p.sliders; let index_6 = 0; while ((index_6 < iter_5.length)) { let s = iter_5[index_6]; (v[s.id] = s.val); (index_6 = ((index_6 + 1))) } }; if ((((!script)) || ((script.raw != p.code)))) { (script = scriptCache.current[cacheKey]) }; if script.valid { pass }; break; };
    };

    let mut handleImport = || {
        if fileInputRef.current { fileInputRef.current.click() };
    };

    let mut handleBake = || {
        if baking { return; };
        let group = meshGroupRef.current;
        if ((((!group)) || ((group.children.length == 0)))) { setStatus("Error: No clones to export".to_string()); return; };
        setBaking(true);
        setStatus("Exporting clones...".to_string());
    };

    let mut handleAddModifier = |key: String| {
        let newMod = createModifierInstance(key);
        setChains(|prev: Any| prev.map(|c: Any| if ((c.id == activeChainId)) { Any() } else { c }));
        if activeChainId { setSelectedModId(newMod.instanceId) };
    };

    let mut handleAction = |type_: String, payload: Any| {
        if ((type_ == "ADD_CHAIN".to_string())) { setChains(|p: Any| vec![p, createChainInstance(p.length)]) } else if ((type_ == "REMOVE_CHAIN".to_string())) { setChains(|p: Any| p.filter(|c: Any| ((c.id != payload)))); if ((activeChainId == payload)) { setActiveChainId(chains[0].id) } } else if ((type_ == "UPDATE_CHAIN".to_string())) { setChains(|p: Any| p.map(|c: Any| if ((c.id == payload.id)) { Any() } else { c })) } else if ((type_ == "REMOVE_MOD".to_string())) { setChains(|p: Any| p.map(|c: Any| if ((c.id == activeChainId)) { Any() } else { c })) } else if ((type_ == "UPDATE_PARAM".to_string())) { setChains(|p: Any| p.map(|c: Any| if ((c.id == activeChainId)) { Any() } else { c })) };
    };

    let mut setUiTime = |value: Any| {
        uiTime = value;
    };

    AppShell(AppShellProps { menuBar: None, topBar: None, left: Any(), right: Any(), bottom: None, children: vec![format!("<div{}{}</div>", vec![format!(" ref=\"{}\"", mountRef), format!(" className=\"{}\"", "w-full h-full relative group")].join(""), vec![format!("<input{}{}</input>", vec![format!(" type=\"{}\"", "file"), format!(" ref=\"{}\"", fileInputRef), format!(" onChange=\"{}\"", handleImport), format!(" className=\"{}\"", "hidden")].join(""), String::new())].join(""))].join("") })
}

