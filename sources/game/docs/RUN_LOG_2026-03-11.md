# Game Engine Specialist Run Log (2026-03-11)

## Run 1 Summary

Implemented the first executable `sources/game` pipeline layer using a data-driven manifest and a new orchestration crate.

## Run 1 Added

- `crates/k-os-game-runtime`
  - TOML manifest schema for game pipeline stages
  - strict validation rules
  - deterministic execution planner
  - unit tests for edge cases
- `sources/game/config/pipeline.toml`
  - initial pipeline stages:
    - `scene_bootstrap`
    - `kain_hot_reload`
    - `narrative_graph`
  - profile data for editor preview and runtime play
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - ownership model and next increments

## Run 2 Summary

Extended the game pipeline into a data-driven execution-prep layer with preflight checks, runner adapter mapping, and gameplay-focused data registries.

## Run 2 Added

- `crates/k-os-game-runtime`
  - new stage kind: `data_registry`
  - manifest-driven preflight checks with strict/warn mode
  - manifest-driven runner adapter resolution with strict binding mode
  - typed run report for stage adapter planning
  - expanded tests for preflight and runner behavior
- `sources/game/config/pipeline.toml`
  - new `[preflight]` and `[runner]` sections
  - strict adapter map under `[runner.adapters]`
  - new `data_registry` stages:
    - `input_bindings_registry`
    - `gameplay_tags_registry`
    - `actor_archetypes_registry`
    - `camera_rigs_registry`
    - `visual_scripting_registry`
  - expanded dependency chain into `narrative_graph`
- `sources/game/assets/*`
  - initial authored data payloads for scene, narrative, gameplay tags/input/actors/cameras/visual scripting
- `sources/game/scripts/README.md`
  - declared script-source lane for Kain hot reload and visual scripting graph bindings

## Why this matters

`sources/game` now has a stronger engine pipeline contract:
- stage sequencing is deterministic
- stage payloads are typed
- preflight catches missing inputs before runtime
- adapter routing is data-driven and strict when required
- gameplay systems (input, tags, actors, cameras, visual scripting) are represented as declarative data assets, not hardcoded wiring

## Risks and constraints

- Adapter execution is currently planning-only; no runtime host dispatch yet.
- Kain script files referenced by visual scripting assets are planned but not implemented yet.
- Registry schema migration/versioning is not implemented yet.

## Immediate next tasks

1. Build runtime adapter dispatch integration in `k-os-bevy`/game bootstrap.
2. Define registry schema validators + migration hooks.
3. Wire launcher `K_OS Game` boot path through preflight + execution report.
4. Add packaged build/cook/export stage kinds.

## Run 3 Summary

Implemented concrete built-in adapter execution in `k-os-game-runtime` so the pipeline can perform real subsystem preflight dispatch, not only planning.

## Run 3 Added

- `crates/k-os-game-runtime`
  - `RuntimeExecutionOptions` and `execute_plan_with_builtin_adapters(...)`
  - per-stage runtime statuses: `planned`, `succeeded`, `failed`
  - dependency-aware failure propagation (blocked dependent stages marked failed)
  - built-in adapter execution handlers:
    - `scene`: validates `.kscene` JSON schema + actor array
    - `kain_hot_reload`: validates source registry/watch roots and reports missing watchers
    - `data_registry`: validates TOML `registry_kind` + `version`
    - `narrative_graph`: validates entry node exists in graph nodes
  - root-resolution fix: `preflight.workspace_root = ""` now falls back to caller-provided workspace root
  - expanded tests for:
    - successful full built-in adapter run
    - narrative entry-node failure in fail-fast mode
    - blocked dependent stages when fail-fast is disabled
- `crates/k-os-game-runtime/README.md`
  - updated capabilities and execution coverage
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - documented built-in adapter execution guarantees and updated next increments

## Why this matters

`sources/game` now has an executable data-driven runtime preflight lane that validates real scene/script/registry/narrative inputs before host bootstrap integration. This closes a key gap between planning and runtime dispatch.

## New constraints

- Built-in adapters are still preflight validators, not full runtime subsystem mutation/injection into ECS/render state.
- `k-os-bevy` integration is the next ownership bridge to consume execution records as actual host startup actions.

## Run 4 Summary

Implemented manifest-driven data-registry schema policy + migration-hook enforcement in `k-os-game-runtime`, and wired `sources/game` pipeline config to declare explicit per-registry contracts.

## Run 4 Added

- `crates/k-os-game-runtime`
  - new manifest config section:
    - `[data_registry]`
    - `[data_registry.schemas.<registry_kind>]`
  - schema policy model per registry kind:
    - `expected_version`
    - `supported_versions`
    - `required_tables`
    - optional `migration_hook`
  - strict-schema manifest validation:
    - when `strict_schema = true`, every `data_registry` stage must map to a declared schema policy
  - built-in `data_registry` adapter now enforces manifest policy at runtime:
    - rejects unsupported versions
    - requires migration hook when running supported-but-legacy version
    - validates required top-level tables/arrays in registry TOML
  - new tests for:
    - strict policy coverage enforcement
    - migration hook gate for legacy versions
    - required table enforcement
- `sources/game/config/pipeline.toml`
  - added `[data_registry]` policy configuration for:
    - input bindings
    - gameplay tags
    - actor archetypes
    - camera rigs
    - visual scripting
  - declared visual scripting migration hook from `v1` to expected `v2`
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - documented schema policy/migration behavior and updated next increments

## Why this matters

`sources/game` registry validation is now truly data-driven and evolution-ready:
- schema behavior is expressed in manifest data, not baked into adapter code
- each gameplay registry has an explicit compatibility contract
- migration requirements are detectable before host bootstrap
- malformed registry structures are caught early through required-table checks

## Run 5 Summary

Integrated `k-os-game-runtime` execution directly into `k-os-bevy` startup so host boot now runs manifest-driven preflight and adapter execution, then emits structured startup records.

## Run 5 Added

- `crates/k-os-bevy`
  - new dependency on `k-os-game-runtime`
  - startup bootstrap system that executes:
    - manifest load/validation
    - plan build
    - preflight checks
    - built-in adapter execution
  - environment-driven host bootstrap config:
    - `KOS_GAME_PIPELINE_ENABLED`
    - `KOS_GAME_PIPELINE_MANIFEST`
    - `KOS_GAME_PIPELINE_WORKSPACE_ROOT`
    - `KOS_GAME_PIPELINE_FAIL_FAST`
  - typed startup message emission per stage execution record (stage kind/profile/adapter/status/detail)
  - persisted host bootstrap resource state for downstream plugins/systems
  - identical bootstrap wiring for both normal and `--standalone` boot paths
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - added runtime host bootstrap guarantees and updated next increments

## Why this matters

`sources/game` now has a real host-bridge layer:
- runtime host boot is data-driven from pipeline config/environment, not hardcoded flow
- execution records are surfaced as startup messages that downstream runtime systems can consume
- pipeline issues fail early with explicit bootstrap errors before deep viewport systems run

## New constraints

- startup messages are currently logged and exposed as resources; they are not yet mapped into ECS gameplay/runtime mutators.
- launcher routing into this bootstrap lane is not wired yet (still pending next increment).

## Run 6 Summary

Added a data-driven registry ingest lane in `k-os-bevy` so successful `data_registry` pipeline stages now populate a runtime cache resource for downstream systems.

## Run 6 Added

- `crates/k-os-bevy`
  - new `GameDataRegistryState` Bevy resource:
    - `loaded` cache keyed by registry kind
    - `errors` list for ingest issues
  - new per-registry snapshot metadata:
    - stage id
    - profile
    - merge strategy
    - resolved asset path
    - version
    - required table entry counts
  - new bootstrap ingest flow:
    - derives registry stage specs from `ExecutionPlan` `StagePayload::DataRegistry`
    - ingests only successful `data_registry` execution records
    - resolves registry file paths against `workspace_root`
    - parses registry TOML documents
    - validates file `registry_kind` against manifest stage `registry_kind`
    - computes required-table counts from manifest schema policy (`[data_registry.schemas.*].required_tables`)
  - extended bootstrap state metrics:
    - `registry_loaded`
    - `registry_errors`
- `crates/k-os-bevy/Cargo.toml`
  - added `toml` dependency for runtime registry parsing

## Verification

- `cargo check -p k-os-bevy` passed.

## Why this matters

`k-os-bevy` now holds a deterministic, data-driven registry cache at startup instead of only logging execution events. This provides a concrete bridge from pipeline validation into runtime-consumable gameplay data.

## New constraints

- Registry ingestion is currently metadata/cache population only; ECS gameplay/input/camera/visual-script mutators are still pending.

## Run 7 Summary

Added data-driven runtime gameplay mutators in `k-os-bevy` that consume pipeline-loaded registries and derive concrete readiness/binding state for downstream ECS systems.

## Run 7 Added

- `crates/k-os-bevy`
  - new runtime policy resource loaded from environment:
    - `KOS_GAME_RUNTIME_REQUIRED_REGISTRIES`
    - `KOS_GAME_RUNTIME_STRICT_READINESS`
  - new runtime binding resources:
    - `GameRuntimeRegistryBindings`
    - `RuntimeReadinessState`
  - new update system:
    - `reconcile_game_pipeline_runtime_state`
  - registry-to-runtime mutator lane that:
    - consumes `GameDataRegistryState.loaded`
    - parses concrete registry data from loaded registry asset paths
    - derives gameplay-facing summaries:
      - input context action counts
      - gameplay tag domain counts
      - actor archetype IDs
      - camera rig IDs
      - visual graph IDs
    - validates cross-registry links from `hero.default` actor archetype:
      - input context reference must exist
      - camera rig reference must exist
    - emits runtime readiness status (`pending`, `ready`, `degraded`) and detail for host/runtime consumers
  - wired the mutator/readiness lane into both normal and `--standalone` app paths

## Verification

- `cargo check -p k-os-bevy` passed.

## Why this matters

`sources/game` now has a concrete bridge from pipeline-stage success into runtime gameplay state mutation. Downstream systems can consume stable data resources instead of reconstructing registry state ad hoc.

## New constraints

- Launcher product routing for `K_OS Game` is still pending; runtime policy is currently environment-driven at the Bevy host level.

## Run 8 Summary

Extended `k-os-game-runtime` with manifest-driven non-registry stage schema policies so stage payload evolution is now governed by data, not hardcoded checks.

## Run 8 Added

- `crates/k-os-game-runtime`
  - new manifest section:
    - `[stage_schemas]`
    - `[stage_schemas.schemas.<stage_kind>]`
  - new non-registry stage schema policy model:
    - `expected_version`
    - `supported_versions`
    - `required_fields`
    - optional `migration_hook`
  - strict stage-schema coverage gate:
    - when `stage_schemas.strict_schema = true`, every non-`data_registry` stage kind in use must declare a policy
  - stage-level schema-version requirement:
    - stages with declared schema policy must provide `schema_version`
  - built-in adapter execution now validates non-registry stage schema policy before stage dispatch:
    - version compatibility and migration-hook gating
    - required payload-field presence from policy data
    - execution detail now includes stage-schema validation summary
  - new tests for:
    - strict non-registry policy coverage
    - `schema_version` requirement with declared stage policy
    - unsupported stage schema version rejection in built-in execution
- `sources/game/config/pipeline.toml`
  - added `[stage_schemas]` policies for:
    - `scene`
    - `kain_hot_reload`
    - `narrative_graph`
  - added `schema_version = "1"` to non-registry stages:
    - `scene_bootstrap`
    - `kain_hot_reload`
    - `narrative_graph`
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - documented non-registry stage schema policy behavior and updated next increments

## Verification

- `cargo test -p k-os-game-runtime` passed (19/19).

## Why this matters

`sources/game` now has stage-payload evolution contracts that mirror the existing registry schema strategy:
- both registry and non-registry stage kinds are policy-driven from manifest data
- schema drift in stage payloads can be detected as a first-class runtime bootstrap error
- migration requirements for stage payload versions are now expressible in data

## New constraints

- `schema_version` must now be maintained for any non-registry stage kind covered by `[stage_schemas.schemas.*]`.

## Run 9 Summary

Added manifest-driven packaging lanes so `sources/game` now models `cook`, `build`, and `export` as first-class pipeline stages with real authored data.

## Run 9 Added

- `crates/k-os-game-runtime`
  - new stage kinds:
    - `cook`
    - `build`
    - `export`
  - new typed stage payloads and manifest fields for packaging contracts:
    - `recipe_asset`
    - `package_asset`
    - `output_dir`
    - `source_dir`
    - `export_path`
    - `artifact_name`
    - `target_platform`
    - `package_format`
  - built-in adapter validation for packaging lanes:
    - `cook` validates recipe TOML, target-platform match, and referenced input existence
    - `build` validates package TOML, target-platform match, and cooked-dir contract
    - `export` validates package TOML, package-format match, and source-dir existence
  - added packaging execution test covering successful `cook -> build -> export` validation flow
- `sources/game/config/pipeline.toml`
  - added runner adapters for `cook`, `build`, and `export`
  - added stage schema policies for packaging stages
  - added pipeline stages:
    - `cook_windows_content`
    - `build_windows_player`
    - `export_windows_bundle`
- `sources/game/assets/packaging/*`
  - added initial authored packaging manifests:
    - `windows.cook.toml`
    - `windows.build.toml`
    - `windows.export.toml`
- `sources/game/build/*`
  - added output-directory scaffolds for cooked and built player content

## Verification

- `cargo test -p k-os-game-runtime` passed (20/20).

## Why this matters

The game-engine pipeline now extends beyond startup/runtime validation into productization structure. We can express how content is prepared, assembled, and packaged using authored manifests instead of hardcoded build scripts.

## New constraints

- Packaging stages are currently validation-oriented contracts; they do not emit final cooked bundles yet.

## Run 10 Summary

Defined the full intended `k-os-game-*` crate architecture and added a dedicated automation guide so future runs build toward a stable product-layer ownership model.

## Run 10 Added

- `sources/game/docs/CRATE_ARCHITECTURE.md`
  - full game-engine crate map
  - ownership rules for existing domain crates versus missing `k-os-game-*` crates
  - recommended dependency direction
  - anti-patterns and phased build order
- `sources/game/docs/AUTOMATION_GUIDE.md`
  - recurring-run guidance for the game-engine automation loop
  - priority order, vertical-slice rules, verification rules, and immediate backlog
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - linked the new architecture/automation docs
  - added next increment calling for `k-os-game-framework` and `k-os-game-play`
- `automation/FRONTIER_AUTOMATION_PROMPTS.md`
  - added a `K_OS Game` product-layer workstream and a dedicated automation prompt

## Why this matters

`sources/game` now has an explicit product-layer target instead of only a bootstrap/runtime pipeline target. This gives future runs a concrete crate roadmap for gameplay, AI, camera, sequencer, narrative, UI, editor, and build ownership.

## Verification

- No build/test command was necessary; this run was documentation and automation-guidance only.

## Run 11 Summary

Created `k-os-game-framework` and `k-os-game-play`, then extracted the first gameplay-binding/runtime ownership slice out of `k-os-bevy` into those crates.

## Run 11 Added

- `crates/k-os-game-framework`
  - new owner crate for:
    - `GamePipelineBootstrapState`
    - `GameDataRegistryState`
    - `RegistrySnapshot`
    - `ingest_data_registry_stage_results(...)`
- `crates/k-os-game-play`
  - new owner crate for:
    - `GamePipelineRuntimePolicy`
    - `GameRuntimeRegistryBindings`
    - `RuntimeReadinessState`
    - runtime gameplay binding derivation from registry data
    - readiness-state derivation from bootstrap + registry state
- `crates/k-os-bevy`
  - now consumes these crates instead of owning the extracted state and gameplay-binding logic directly
- `Cargo.toml`
  - added the new crates to the workspace

## Verification

- `cargo check -p k-os-bevy` passed.

## Why this matters

This is the first real move from architecture intent into product-layer ownership. `k-os-bevy` is now more clearly a host, while the new `k-os-game-*` crates have started becoming the engine’s actual gameplay spine.

## Run 12 Summary

Moved runtime gameplay readiness policy to manifest-driven data in the game pipeline, with environment overrides as an explicit deployment layer.

## Run 12 Added

- `crates/k-os-game-runtime`
  - added `[runtime_policy]` support to `PipelineManifest` via `RuntimePolicyConfig`:
    - `required_registry_kinds`
    - `strict_readiness`
  - added manifest validation for runtime policy shape (non-empty required registry kinds, no empty entries)
  - added tests for:
    - default runtime policy behavior when section is omitted
    - rejecting empty runtime policy entries
- `crates/k-os-game-play`
  - added `GamePipelineRuntimePolicy::from_manifest(...)`
  - added `GamePipelineRuntimePolicy::from_manifest_with_env_overrides(...)`
  - normalized/trimmed/deduplicated required registry kinds with safe fallback defaults
- `crates/k-os-bevy`
  - bootstrap now loads runtime policy from manifest after manifest parse
  - `KOS_GAME_RUNTIME_*` env vars now override manifest policy instead of being the only source
  - removed stale local `DataRegistryStageSpec` host type no longer used by this lane
- `sources/game/config/pipeline.toml`
  - added `[runtime_policy]` with required registry kinds and strict readiness defaults
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - documented runtime policy precedence: manifest first, env override second

## Verification

- `cargo test -p k-os-game-runtime` passed.
- `cargo check -p k-os-game-play` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

Runtime readiness policy is now data-owned by the game pipeline manifest instead of scattered host defaults. This improves portability across editor/runtime profiles and keeps policy evolution in one declarative source.

## Run 13 Summary

Converted packaging stages from validation-only checks into artifact-emitting built-in adapters so `cook/build/export` now generate deterministic pipeline outputs.

## Run 13 Added

- `crates/k-os-game-runtime`
  - extended `RuntimeExecutionOptions` with `emit_artifacts` (default `true`)
  - `cook` adapter now emits `cook.contract.json` (or `[cook].contract_asset`) into stage `output_dir`
  - `build` adapter now emits:
    - artifact directory under `output_dir/<artifact_name>`
    - runtime executable placeholder (from `[runtime].executable` when provided)
    - build contract file (default `build.contract.json`, override via `[bundle].contract_asset`)
  - `export` adapter now emits export artifact payload directly at stage `export_path`
  - added helper for deterministic JSON artifact serialization
  - extended packaging test coverage to assert contract/executable/export artifact paths exist
- `crates/k-os-bevy`
  - bootstrap config now supports `KOS_GAME_PIPELINE_EMIT_ARTIFACTS` (default `true`)
  - runtime execution options now pass `emit_artifacts` into `k-os-game-runtime`
- `sources/game/assets/packaging/*`
  - `windows.cook.toml`: added `[cook].contract_asset`
  - `windows.build.toml`: added `[bundle].contract_asset`
- `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - documented packaging artifact emission behavior and new host env toggle

## Verification

- `cargo test -p k-os-game-runtime` passed (22/22).
- `cargo check -p k-os-bevy` passed.

## Why this matters

The pipeline now produces concrete packaging outputs from manifest data, creating a stable handoff surface for upcoming platform-specific cook/build/export integrations.

## Run 14 Summary

Created `k-os-game-ai` as a new product-owner crate and wired an end-to-end data-driven AI registry lane through `sources/game` pipeline and `k-os-bevy` runtime state reconciliation.

## Run 14 Added

- `crates/k-os-game-ai`
  - new owner crate for AI runtime projection from pipeline-loaded registries
  - `GameAiRuntimeState` Bevy resource with readiness/status/detail fields
  - `derive_ai_runtime_state(...)` for runtime reconciliation from:
    - `GamePipelineBootstrapState`
    - `GameDataRegistryState`
  - AI registry contract validation:
    - requires `state_trees`, `blackboards`, and `agents`
    - validates duplicate IDs and malformed entries
    - validates `agents[*].state_tree` references existing state trees
    - validates `agents[*].blackboard` references existing blackboards
  - tests for:
    - ready state from valid registry
    - degraded state for broken state-tree references
    - pending state when pipeline is disabled
- `crates/k-os-bevy`
  - added dependency on `k-os-game-ai`
  - added runtime reconciliation system for AI state:
    - `reconcile_game_pipeline_ai_state`
  - inserts and updates `GameAiRuntimeState` in both normal and standalone boot paths
  - emits AI readiness logs for ready/degraded transitions
- `sources/game/assets/gameplay/ai.registry.toml`
  - added first authored AI registry data (`state_trees`, `blackboards`, `agents`)
- `sources/game/config/pipeline.toml`
  - added `ai_agents` to `[runtime_policy].required_registry_kinds`
  - added AI registry asset path to `[preflight].required_paths`
  - added `[data_registry.schemas.ai_agents]` policy contract
  - added `ai_agents_registry` stage wired into the gameplay registry dependency chain
- `crates/k-os-game-play`
  - updated env-default required registry kinds to include `ai_agents`
- `Cargo.toml`
  - added `crates/k-os-game-ai` workspace member

## Verification

- `cargo test -p k-os-game-ai` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

The game-engine product layer now has a dedicated AI owner crate with a concrete runtime projection path, rather than keeping AI as an implicit or host-local concern. This keeps crate ownership aligned with the architecture plan while extending the pipeline with another data-driven runtime lane.

## New constraints

- AI ownership is currently registry projection and contract validation only; behavior-tree ticking, blackboard mutation systems, and gameplay query integration remain pending.

## Run 15 Summary

Added deterministic packaging provenance metadata to `k-os-game-runtime` so emitted `cook/build/export` artifacts now carry source hash and registry-version lineage.

## Run 15 Added

- `crates/k-os-game-runtime`
  - added SHA-256 hashing support for artifact provenance
  - `cook` artifact emission now records:
    - recipe hash
    - input asset hashes
    - registry version chain derived from `.registry.toml` inputs
  - `build` artifact emission now records:
    - package + runtime executable hashes
    - upstream source contract hashes discovered in cooked output
    - propagated registry versions from upstream contracts
  - `export` artifact emission now records:
    - package hash
    - upstream source contract hashes discovered in source bundle
    - propagated registry versions from upstream contracts
  - added recursive contract discovery helpers and provenance dedupe utilities
  - extended packaging integration test to assert provenance fields and registry lineage propagation
- `crates/k-os-game-runtime/Cargo.toml`
  - added `sha2` dependency
- docs
  - `crates/k-os-game-runtime/README.md`
  - `sources/game/docs/PIPELINE_ARCHITECTURE.md`

## Verification

- `cargo test -p k-os-game-runtime` passed (22/22).

## Why this matters

The packaging lane now has a deterministic provenance chain for reproducibility and traceability across `cook -> build -> export`, with registry schema/version lineage carried into final export artifacts.

## New constraints

- Provenance hashing currently reads full files into memory (simple and deterministic); very large artifacts may need streaming hash support in a future run.

## Run 16 Summary

Extracted launcher/profile preset ownership from `k-os-bevy` into `k-os-game-framework` and wired host bootstrap to consume the crate-owned preset contract.

## Run 16 Added

- `crates/k-os-game-framework`
  - added `GameProductLaunchPreset` resource contract.
  - added `resolve_game_product_launch_preset(...)`:
    - resolves product identity + profile contract from manifest data.
    - supports explicit requested profile.
    - defaults to `editor_preview` when available, otherwise first manifest profile.
    - fails fast with explicit error when requested profile is missing.
  - added tests for explicit profile resolution, default fallback behavior, and missing-profile rejection.
- `crates/k-os-bevy`
  - bootstrap config now supports `KOS_GAME_PIPELINE_PROFILE`.
  - bootstrap now resolves launch preset through `k-os-game-framework` and inserts `GameProductLaunchPreset` as a resource.
  - startup now fails early with structured bootstrap error when launch preset resolution is invalid.
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` to reflect launcher/profile ownership in `k-os-game-framework`.

## Verification

- `cargo test -p k-os-game-framework` passed (3/3).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Launcher/profile selection is no longer an ad hoc host-local concern. Product identity and profile contracts now resolve in a game-product owner crate, which reduces `k-os-bevy` product-layer ownership and keeps launch behavior data-driven from manifest profiles.

## New constraints

- `KOS_GAME_PIPELINE_PROFILE` must match an existing `[profiles.<id>]` entry in the pipeline manifest when provided.

## Run 17 Summary

Expanded `k-os-game-ai` from registry projection into a deterministic, data-driven decision tick + blackboard mutation runtime lane, and wired it into `k-os-bevy` update flow.

## Run 17 Added

- `crates/k-os-game-ai`
  - extended `GameAiRuntimeState` with runtime-owned AI contracts:
    - state-tree roots
    - blackboard key maps
    - per-agent state-tree/blackboard bindings
    - tick policy (`tick_phase_order`, `default_blackboard_value_template`)
    - per-agent phase decision overrides
  - new `GameAiDecisionRuntimeState` Bevy resource for tick output:
    - `tick_index`
    - `active_phase`
    - per-agent decisions/active nodes
    - per-blackboard key-value state
  - new `derive_ai_decision_runtime_state(...)`:
    - deterministic phase rotation by configured `phase_order`
    - default decision derivation from state-tree root + phase
    - decision override application from `[[decision_overrides]]`
    - template-driven blackboard value mutation using `{agent}`, `{phase}`, `{tick}`, `{decision}`, `{key}`
  - upgraded registry parsing/validation to enforce:
    - `state_trees[*].root`
    - `blackboards.<id>.keys[]`
    - optional `[runtime_tick]` policy contract
    - optional `[[decision_overrides]]` phase validity
  - new tests for decision tick rotation, override behavior, and degraded fallback behavior
- `crates/k-os-bevy`
  - new `GameAiDecisionRuntimeState` resource insertion in both normal and standalone boot paths
  - new update system `reconcile_game_pipeline_ai_decision_state` to tick AI runtime state each frame from `GameAiRuntimeState`
- `sources/game/assets/gameplay/ai.registry.toml`
  - added `[runtime_tick]` policy data
  - added `[[decision_overrides]]` entries for `hero.default` and `npc.vendor`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` for decision runtime ownership and next increment
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to reflect `k-os-game-ai` existing status and new ownership slice

## Verification

- `cargo test -p k-os-game-ai` passed (5/5).
- `cargo check -p k-os-bevy` passed.

## Why this matters

AI is now a live runtime system lane, not only startup validation metadata. Decisions and blackboard mutations are authored as data and evaluated deterministically per tick, which creates a stable bridge into upcoming gameplay query/action systems.

## New constraints

- Decision ticking currently derives outcomes from registry policy templates and overrides; it does not yet execute behavior-tree transitions or world-query-driven scoring.

## Run 18 Summary

Routed `GameAiDecisionRuntimeState` into a data-driven gameplay action projection lane owned by `k-os-game-play`, then wired it into `k-os-bevy` runtime updates.

## Run 18 Added

- `crates/k-os-game-play`
  - new `GameRuntimeActionBindings` Bevy resource for projected gameplay actions.
  - new `derive_runtime_action_bindings(...)` runtime lane that consumes:
    - `GameAiDecisionRuntimeState`
    - `GameDataRegistryState`
    - `GameRuntimeRegistryBindings`
  - new actor-registry route contract parser for `[ai_action_projection]` with `[[ai_action_projection.routes]]`.
  - deterministic route selection per agent decision:
    - highest `priority`
    - longest `decision_prefix` tie-break
  - validation for optional route contracts:
    - `input_action` must exist in `input_bindings` registry when available
    - `camera_rig` must exist in loaded camera rig bindings when available
  - added unit tests for:
    - successful decision-to-action projection
    - no-route degradation behavior
    - pending behavior when AI decision state is unavailable
- `crates/k-os-bevy`
  - inserted `GameRuntimeActionBindings` resource in normal and standalone boot paths.
  - added update system `reconcile_game_pipeline_action_state`.
  - wired action-state reconciliation to run after AI decision reconciliation.
- `sources/game/assets/gameplay/actors.registry.toml`
  - added `[ai_action_projection]` data with four authored routes covering hero and vendor decision domains.
- `sources/game/config/pipeline.toml`
  - strengthened `actor_archetypes` schema contract:
    - `required_tables = ["archetypes", "ai_action_projection"]`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` for new gameplay action projection ownership.

## Verification

- `cargo test -p k-os-game-play` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

AI decision ticks now feed a concrete gameplay action projection layer through authored data contracts instead of host-local conditionals. This closes the previous gap between AI runtime state and gameplay execution readiness.

## New constraints

- Action projection currently resolves action IDs and hints; it does not yet execute gameplay actions against ECS/world state.

## Run 19 Summary

Added a framework-owned launch preset matrix so launcher preset IDs now carry runtime-policy defaults, reducing host-local policy coupling in `k-os-bevy`.

## Run 19 Added

- `crates/k-os-game-framework`
  - expanded `GameProductLaunchPreset` with:
    - `preset_id`
    - `runtime_required_registry_kinds`
    - `runtime_strict_readiness`
  - added `GameProductLaunchPresetMatrix` and `build_game_product_launch_preset_matrix(...)`.
  - upgraded `resolve_game_product_launch_preset(...)` to support:
    - explicit preset id selection
    - profile-id fallback to `<profile>.default`
    - default resolution to `editor_preview.default` when present
  - added tests for:
    - preset-id alias resolution
    - runtime-policy default bundling in preset matrix
- `crates/k-os-game-play`
  - added `GamePipelineRuntimePolicy::from_launch_preset_with_env_overrides(...)`.
  - refactored env override application into shared policy helper.
- `crates/k-os-bevy`
  - bootstrap config now supports `KOS_GAME_PIPELINE_PRESET`.
  - launch preset resolution now passes both requested preset id and requested profile.
  - runtime policy now derives from selected launch preset defaults (then applies env overrides), instead of directly from manifest.
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` to reflect preset-matrix ownership and policy flow.

## Verification

- `cargo test -p k-os-game-framework` passed (5/5).
- `cargo check -p k-os-game-play` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

Launcher selection is now expressed as a preset contract that includes runtime readiness policy defaults. This makes profile/policy composition explicit in a product owner crate and keeps `k-os-bevy` focused on host orchestration.

## New constraints

- `KOS_GAME_PIPELINE_PRESET` must match an available preset id (`<profile>.default` or profile alias) when provided.

## Run 20 Summary

Created `k-os-game-camera` as a dedicated product-owner crate and wired a data-driven camera-runtime lane from gameplay action projection into `k-os-bevy`.

## Run 20 Added

- `crates/k-os-game-camera`
  - new owner crate for camera-runtime projection from registry + gameplay runtime state.
  - `GameCameraRuntimeState` Bevy resource with:
    - active/previous rig state
    - transition blend timing
    - selected route metadata
    - registry/version/runtime diagnostics
  - new `derive_runtime_camera_state(...)` consuming:
    - `GameDataRegistryState` (`camera_rigs` registry snapshot)
    - `GameRuntimeRegistryBindings` (default rig fallback)
    - `GameRuntimeActionBindings` (agent gameplay actions/camera hints)
    - previous `GameCameraRuntimeState` for deterministic transition continuity
  - camera registry contract parsing/validation:
    - required `[[rigs]]` entries (`id`, `mode`)
    - optional `[action_projection]` with `[[action_projection.routes]]`:
      - required: `gameplay_action_prefix`, `target_rig`
      - optional: `agent`, `phase`, `blend_seconds`, `priority`
  - deterministic route and fallback behavior:
    - route ranking by `priority` then longest action prefix
    - fallback chain: gameplay camera hints -> runtime default player rig -> previous rig -> first declared rig
  - unit tests for:
    - action-routed cinematic rig selection
    - runtime-default rig fallback
    - degradation on invalid route target rig
    - pending behavior without registry state
- `crates/k-os-bevy`
  - added dependency on `k-os-game-camera`.
  - inserts `GameCameraRuntimeState` resource in normal and standalone boot paths.
  - added update system `reconcile_game_pipeline_camera_state`.
  - wired camera reconciliation to run after gameplay action projection.
- `sources/game/assets/gameplay/cameras.registry.toml`
  - added `[action_projection]` route data authored for hero combat/sense, vendor interaction, and recover transitions.
- `sources/game/config/pipeline.toml`
  - strengthened `camera_rigs` schema policy:
    - `required_tables = ["rigs", "action_projection"]`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` for camera runtime ownership and contracts.
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to mark `k-os-game-camera` as existing with its first ownership slice.

## Verification

- `cargo test -p k-os-game-camera` passed (4/4).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Camera behavior is now a first-class, data-driven product lane with a dedicated owner crate rather than being implicit in host logic or gameplay route metadata alone. This aligns with the architecture roadmap while establishing deterministic camera routing and transition policy that can expand into advanced camera stack/rails/effects systems.

## New constraints

- `camera_rigs` registry now requires `[action_projection]` under strict schema policy.
- Camera route matching currently keys on gameplay action prefixes + optional `agent`/`phase`; richer world-state-driven camera scoring remains future work.

## Run 21 Summary

Created `k-os-game-input` as a dedicated product-owner crate and wired a data-driven input-runtime lane from gameplay action projection into `k-os-bevy`.

## Run 21 Added

- `crates/k-os-game-input`
  - new owner crate for input-runtime projection from registry + gameplay runtime state.
  - `GameInputRuntimeState` Bevy resource with:
    - active context selection
    - per-agent resolved input actions
    - action kind/default binding cache
    - route diagnostics and issues
  - new `derive_runtime_input_state(...)` consuming:
    - `GameDataRegistryState` (`input_bindings` registry snapshot)
    - `GameRuntimeRegistryBindings` (default player input context fallback)
    - `GameRuntimeActionBindings` (agent gameplay actions + optional input-action hints)
  - input registry contract parsing/validation:
    - required `[[contexts]]` and `[[contexts.actions]]`
    - optional `[action_projection]` with `[[action_projection.routes]]`:
      - required: `gameplay_action_prefix`, `input_action`
      - optional: `agent`, `phase`, `context`, `priority`
  - deterministic routing behavior:
    - prefers `GameRuntimeActionBindings.agent_input_actions`
    - route fallback by highest `priority` then longest gameplay-action prefix
    - active-context fallback: runtime default context -> highest-priority context
  - unit tests for:
    - mixed direct + route-projected input-action resolution
    - degraded behavior for invalid route action references
    - pending behavior without gameplay action runtime state
- `crates/k-os-bevy`
  - added dependency on `k-os-game-input`.
  - inserts `GameInputRuntimeState` resource in normal and standalone boot paths.
  - added update system `reconcile_game_pipeline_input_state`.
  - wired input reconciliation to run after gameplay action projection and before camera reconciliation.
- `sources/game/assets/gameplay/input.registry.toml`
  - added `[action_projection]` route data for vendor interaction and recovery locomotion mappings.
- `sources/game/config/pipeline.toml`
  - strengthened `input_bindings` schema policy:
    - `required_tables = ["contexts", "action_projection"]`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` for input runtime ownership and contracts.
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to mark `k-os-game-input` as existing with its first ownership slice.

## Verification

- `cargo test -p k-os-game-input` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

Input is now a first-class product runtime lane with a dedicated owner crate. Gameplay action projection can be deterministically translated into input-runtime contracts from authored registry data instead of remaining host-local glue.

## New constraints

- `input_bindings` registry now requires `[action_projection]` under strict schema policy.
- Route selection currently keys on gameplay action prefixes + optional `agent`/`phase`/`context`; richer trigger/modifier evaluation remains future work.

## Run 22 Summary

Expanded `k-os-game-input` into a camera-coupled policy lane so gameplay input mapping can be gated/scaled by authored camera mode data.

## Run 22 Added

- `crates/k-os-game-input`
  - added dependency on `k-os-game-camera` and updated `derive_runtime_input_state(...)` to consume `GameCameraRuntimeState`.
  - extended `GameInputRuntimeState` with camera-policy outputs:
    - `active_camera_rig`
    - `active_camera_mode`
    - `camera_policy_rules_loaded`
    - `agent_camera_modes`
    - `agent_action_scales`
    - `agent_gates`
  - added new registry-driven policy parser:
    - optional `[input_policy]`
    - optional `[[input_policy.camera_mode_aliases]]` (`rig_prefix`, `mode`)
    - optional `[[input_policy.rules]]` matching on `agent`/`phase`/`camera_mode`/`gameplay_action_prefix`/`input_action`
    - policy behaviors:
      - `allow = false` gates resolved mappings with authored reason
      - `scale` emits per-agent input scale modifiers for downstream trigger/modifier systems
  - deterministic policy ranking:
    - highest `priority`
    - longest `gameplay_action_prefix`
    - explicit `input_action` match tie-break
  - added unit test `applies_camera_policy_gate_and_scale_rules`.
- `crates/k-os-bevy`
  - updated `reconcile_game_pipeline_input_state` to pass camera runtime state into `derive_runtime_input_state(...)`.
  - adjusted update ordering to ensure current-frame camera policy data is available:
    - `reconcile_game_pipeline_action_state`
    - `reconcile_game_pipeline_camera_state`
    - `reconcile_game_pipeline_input_state`
  - expanded input runtime logging to include gated count and active camera mode.
- `sources/game/assets/gameplay/input.registry.toml`
  - added authored `[input_policy]` data:
    - camera mode aliases for third-person/cinematic/freefly rigs
    - cinematic combat gate rule
    - third-person look-scale modifier rule
- `sources/game/config/pipeline.toml`
  - strengthened `input_bindings` schema policy:
    - `required_tables = ["contexts", "action_projection", "input_policy"]`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with camera-coupled input policy contract and deterministic evaluation rules.
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to include `k-os-game-input` camera-policy ownership slice.

## Verification

- `cargo test -p k-os-game-input` passed (4/4).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Input routing is now genuinely context-aware across gameplay phase and camera mode, allowing authored cinematic lockouts and action scaling without hardcoded branch logic in host systems.

## New constraints

- `input_bindings` registry now requires `[input_policy]` under strict schema policy.
- Camera mode resolution depends on `input_policy.camera_mode_aliases` prefix quality; ambiguous prefixes should be avoided.

## Run 23 Summary

Added a host-event application lane in `k-os-game-input` so raw host input is now projected into deterministic gameplay input intents using authored registry policy data.

## Run 23 Added

- `crates/k-os-game-input`
  - added `GameHostInputFrame` resource contract (host-captured digital tokens + axis vectors).
  - added `GameInputIntentState` resource contract and `GameAgentInputIntent` payload for per-agent intent output.
  - added `apply_host_input_frame(...)` runtime lane:
    - consumes `GameInputRuntimeState` + `GameHostInputFrame`.
    - applies existing per-agent gates/scales from camera-coupled policy.
    - resolves intent activation/strength for `digital` and `axis_2d` actions.
  - added `input_host` registry contract parsing:
    - `axis_deadzone`
    - `activation_threshold`
    - `max_mouse_delta`
    - `[[input_host.digital_axes]]` token-to-axis mapping
  - expanded tests for:
    - digital-axis map projection into axis intents
    - gated runtime producing no host intents
- `crates/k-os-bevy`
  - captures host keyboard/mouse input into `GameHostInputFrame` each update.
  - applies host frame to `GameInputIntentState` after `GameInputRuntimeState` reconciliation.
  - inserts `GameHostInputFrame` and `GameInputIntentState` resources in normal and standalone boot paths.
- `sources/game` data/contracts
  - `sources/game/assets/gameplay/input.registry.toml` now includes authored `[input_host]` policy and `digital_axes`.
  - `sources/game/config/pipeline.toml` now requires `input_bindings` table `input_host`.
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` for the new host-event lane and ownership details.

## Verification

- `cargo test -p k-os-game-input` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

The input stack now has an end-to-end data-driven path from host device events to gameplay-consumable intent state. This closes the previous gap where input routing existed but no crate-owned lane translated real host input into deterministic agent intent output.

## New constraints

- `input_bindings` registry now requires `[input_host]` under strict schema policy.
- Host token coverage is currently keyboard + mouse focused in `k-os-bevy`; gamepad token capture can be added as a follow-up lane.

## Run 24 Summary

Added a data-driven runtime execution-command lane so pipeline-derived input intents now drive concrete world mutations in `k-os-bevy`.

## Run 24 Added

- `crates/k-os-game-input`
  - added `GameExecutionCommandState` + `GameAgentExecutionCommand` runtime resources.
  - added `derive_execution_command_state(...)` that projects `GameInputIntentState` into execution commands from authored `[runtime_execution.routes]` in `input_bindings`.
  - added deterministic route ranking for execution routes:
    - highest `priority`
    - explicit `input_kind` match tie-break
    - explicit `phase` match tie-break
    - explicit `agent` match tie-break
  - added execution route schema support:
    - required: `input_action`, `command_kind`
    - optional: `agent`, `phase`, `input_kind`, `vector_space`, `linear_scale`, `angular_scale`, `active_only`, `priority`
  - added tests for:
    - successful intent-to-command projection
    - degraded projection when no route matches intent
- `crates/k-os-bevy`
  - added pipeline systems:
    - `reconcile_game_pipeline_execution_commands`
    - `apply_game_pipeline_execution_commands`
  - wired update order to run execution reconciliation after input-intent reconciliation.
  - applied execution commands to runtime agent anchors as concrete world mutations (`Transform` translation/rotation).
  - added runtime anchor components/resources:
    - `GameRuntimeAgentAnchor`
    - `GameRuntimeAgentMotion`
    - `GameExecutionCommandState` insertion in normal + standalone paths.
  - added default runtime anchor entities for `hero.default` and `npc.vendor` in `setup_scene`.
- `sources/game` data/config
  - `sources/game/assets/gameplay/input.registry.toml` now includes `[runtime_execution]` authored routes for move/look/interact command lanes.
  - `sources/game/config/pipeline.toml` now requires `input_bindings` table `runtime_execution` under strict schema policy.
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with runtime execution projection + world mutation lane.
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` with `k-os-game-input` execution-command ownership slice.

## Verification

- `cargo test -p k-os-game-input` passed.
- `cargo check -p k-os-bevy` passed.

## Why this matters

The pipeline now has a concrete, data-driven bridge from host input through gameplay intent into runtime world mutation, closing the previous gap where intent state existed but did not deterministically drive ECS-visible behavior.

## New constraints

- `input_bindings` registry now requires `[runtime_execution]` under strict schema policy.
- Current world mutation application is host-owned in `k-os-bevy`; a future increment should move execution ownership into a dedicated gameplay owner crate.

## Run 25 Summary

Moved runtime execution-command ownership into `k-os-game-play`, extracted shared runtime binding contracts into `k-os-game-framework`, and added a thin live diagnostics UI for pipeline/runtime inspection.

## Run 25 Added

- `crates/k-os-game-framework`
  - now owns shared gameplay runtime contracts:
    - `GameRuntimeRegistryBindings`
    - `GameRuntimeActionBindings`
  - this removes product-layer coupling between `k-os-game-input` / `k-os-game-camera` and `k-os-game-play`.
- `crates/k-os-game-play`
  - now owns:
    - `GameExecutionCommandState`
    - `GameAgentExecutionCommand`
    - `derive_execution_command_state(...)`
  - consumes `GameInputIntentState` from `k-os-game-input` plus authored `[runtime_execution.routes]`.
  - added tests for:
    - successful intent-to-command projection
    - degraded projection when no route matches intent
- `crates/k-os-game-input`
  - now stops owning execution-command projection and focuses on:
    - input runtime mapping
    - host input capture contract
    - host input intent projection
- `crates/k-os-game-camera`
  - now consumes shared runtime binding contracts from `k-os-game-framework`.
- `crates/k-os-bevy`
  - updated imports/wiring to use play-owned execution-command state.
  - added lightweight egui diagnostics window `Game Runtime`.
  - diagnostics now show:
    - bootstrap/readiness
    - AI + action + camera + input + intent + execution-command state
    - live agent anchor transform/motion values
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md`
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md`
  - updated `sources/game/docs/RUN_LOG_2026-03-11.md` (Run 25 section).

## Verification

- `cargo test -p k-os-game-play` passed (5/5).
- `cargo test -p k-os-game-input` passed (6/6).
- `cargo test -p k-os-game-camera` passed (4/4).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Execution dispatch now sits in the gameplay owner crate instead of the input crate, which is a much cleaner product boundary. The runtime diagnostics UI also gives us a usable live inspection surface before `k-os-game-ui` exists as a full product owner.

## New constraints

- Command application is still host-applied in `k-os-bevy`; only command projection ownership moved to `k-os-game-play`.
- The diagnostics UI is intentionally thin and inspection-only; it is not the final `k-os-game-ui` architecture.

## Run 26 Summary

Moved execution-command application semantics into `k-os-game-play` with a deterministic, data-driven fixed-step lane, and reduced `k-os-bevy` to a host-side application adapter.

## Run 26 Added

- `crates/k-os-game-play`
  - added `GameExecutionApplicationState` + `GameAgentExecutionApplication` runtime resources.
  - added `derive_execution_application_state(...)` consuming:
    - `GameExecutionCommandState`
    - `GameDataRegistryState` (`input_bindings` snapshot)
    - previous application state for deterministic decay/step continuity.
  - added authored-configuration support for `[runtime_execution_apply]` in `input_bindings`:
    - `fixed_step_seconds`
    - `max_linear_speed`
    - `max_angular_speed`
    - `max_frame_steps`
    - `inactive_decay`
  - gameplay application lane now:
    - clamps linear/angular velocity magnitudes by policy
    - applies bounded fixed-step frame integration independent from host frame delta
    - decays inactive commands from previous application velocity for deterministic falloff
    - emits per-agent translation/rotation deltas and velocities for host adapters
  - added tests:
    - fixed-step apply policy + clamping behavior
    - inactive velocity decay behavior
- `crates/k-os-bevy`
  - added `reconcile_game_pipeline_execution_application` system.
  - now inserts/updates `GameExecutionApplicationState` in normal + standalone boot paths.
  - `apply_game_pipeline_execution_commands` now consumes gameplay-owned application state (`GameExecutionApplicationState`) rather than doing command math from `GameExecutionCommandState` + host `Time`.
  - runtime diagnostics now show execution-application status (`applied`, `steps`, `frame`).
- `sources/game` data/contracts
  - `sources/game/assets/gameplay/input.registry.toml` now includes authored `[runtime_execution_apply]` values.
  - `sources/game/config/pipeline.toml` now requires `input_bindings` table `runtime_execution_apply` under strict schema policy.
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` to reflect gameplay-owned execution application semantics.

## Verification

- `cargo test -p k-os-game-play` passed (7/7).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Execution application semantics are now authored and owned by gameplay crates instead of host-frame timing in `k-os-bevy`, which is a cleaner architecture for deterministic simulation, replay/replication readiness, and future multi-host runtime adapters.

## New constraints

- `input_bindings` registry now requires `[runtime_execution_apply]` under strict schema policy.
- The current application lane is still kinematic and transform-centric; richer replicated command families (ability/effect/impulse/event channels) should be layered on top of this policy-driven base.

## Run 27 Summary

Created `k-os-game-sequencer` as the new Phase 2 owner crate and wired a data-driven sequencer timeline lane through `sources/game` pipeline manifests and `k-os-bevy` runtime reconciliation.

## Run 27 Added

- `crates/k-os-game-sequencer`
  - new `GameSequencerRuntimeState` resource for sequencer-runtime ownership.
  - added `derive_runtime_sequencer_state(...)` consuming:
    - `GameDataRegistryState` (`sequencer_timelines` snapshot)
    - `GameRuntimeActionBindings` (agent gameplay actions + active phase)
    - `GameCameraRuntimeState` (active rig/mode filters)
    - previous sequencer state for deterministic fallback continuity.
  - added sequencer registry contract support:
    - required `[[timelines]]` entries with `id`
    - optional `[action_projection]` route table with deterministic route ranking
    - optional `[playback_policy]` defaults
  - deterministic fallback chain for active timeline:
    - matching action route
    - `playback_policy.default_timeline`
    - previous active timeline (if still declared)
    - first declared timeline
  - added tests for:
    - successful gameplay/camera-filtered route projection
    - playback-policy fallback behavior
    - degraded state on missing target timeline
    - previous-timeline continuity fallback
- `crates/k-os-bevy`
  - added dependency on `k-os-game-sequencer`.
  - added `reconcile_game_pipeline_sequencer_state` runtime system.
  - inserted `GameSequencerRuntimeState` resource in normal + standalone boot paths.
  - updated runtime update ordering:
    - sequencer reconciliation now runs after camera reconciliation and before input reconciliation.
  - extended runtime diagnostics views to include sequencer status (`timeline`, `routes`, `events`).
- `sources/game` data/config
  - added `sources/game/assets/gameplay/sequencer.registry.toml` as the first authored sequencer timeline registry.
  - updated `sources/game/config/pipeline.toml`:
    - added `sequencer_timelines` to `[runtime_policy].required_registry_kinds`
    - added preflight required path for the sequencer registry asset
    - added strict schema policy for `sequencer_timelines`
    - added `sequencer_timelines_registry` data-registry stage
    - updated stage dependencies so `visual_scripting_registry` depends on `sequencer_timelines_registry`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with sequencer runtime guarantees and registry asset coverage.
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to mark `k-os-game-sequencer` as existing and document its first ownership slice.

## Verification

- `cargo test -p k-os-game-sequencer` passed (4/4).
- `cargo check -p k-os-bevy` passed.

## Why this matters

`K_OS Game` now has an explicit sequencer owner crate and a data-driven runtime timeline lane, which is a core Phase 2 differentiator toward cinematic/gameplay integration without hardcoding host logic in `k-os-bevy`.

## New constraints

- `sequencer_timelines` is now part of strict runtime registry readiness.
- Sequencer projection is currently route/policy driven only; track evaluation and event dispatch execution are the next expansion lane.


## Run 28 Summary

Added a gameplay-owned execution-effects lane that turns applied execution commands into deterministic, channelized effect events (`movement`, `impulse`, `ability_cast`, `interaction`) for replication/replay readiness.

## Run 28 Added

- `crates/k-os-game-play`
  - added new runtime resource/state:
    - `GameExecutionEffectsState`
    - `GameExecutionEffectEvent`
  - added `derive_execution_effects_state(...)` consuming:
    - `GameExecutionCommandState`
    - `GameExecutionApplicationState`
    - `input_bindings` registry `[runtime_execution_effects]` policy
    - previous effects state for deterministic sequence continuity
  - added policy parsing for `[runtime_execution_effects]`:
    - `max_events_per_frame`
    - `default_replication_channel`
    - `[[runtime_execution_effects.routes]]` with route ranking (`priority` + longest `command_prefix` + explicit `phase`/`agent` tie-break)
  - added deterministic effect-event emission with:
    - per-event monotonic sequence IDs
    - per-route `replication_channel` and `effect_kind`
    - bounded per-frame event clamp (`max_events_per_frame`)
    - optional inactive command emission (`emit_inactive`)
  - added tests:
    - deterministic sequence + route channel/effect projection
    - event clamp + inactive emission policy behavior
- `crates/k-os-bevy`
  - added `GameExecutionEffectsState` resource wiring in normal + standalone boot paths
  - added `reconcile_game_pipeline_execution_effects` runtime system
  - updated runtime system ordering:
    - execution effects now reconcile after execution application and before host transform apply
  - extended runtime diagnostics panel to show execution-effects status (`events`, `routes`, `next_seq`)
- `sources/game` data/contracts
  - `sources/game/assets/gameplay/input.registry.toml` now includes authored `[runtime_execution_effects]` and route entries for locomotion/camera/interact/ability lanes
  - `sources/game/config/pipeline.toml` now requires `input_bindings` table `runtime_execution_effects` under strict schema policy
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` and `sources/game/docs/CRATE_ARCHITECTURE.md` for execution-effects ownership/contracts

## Verification

- `cargo test -p k-os-game-play` passed (9/9).
- `cargo check -p k-os-bevy` passed.

## Why this matters

The execution pipeline now has a deterministic bridge from simulation/application state into transport-friendly effect events with explicit channels and family typing, which is foundational for rollback-safe networking, replay capture, and multi-host runtime adapters.

## New constraints

- `input_bindings` registry now requires `[runtime_execution_effects]` under strict schema policy.
- Execution effects are currently surfaced as gameplay-owned runtime state and diagnostics; transport/network sinks are the next owner slice.


## Run 29 Summary

Expanded sequencer ownership from timeline projection into deterministic runtime track dispatch and event emission using a data-driven `[runtime_dispatch]` contract.

## Run 29 Added

- `crates/k-os-game-sequencer`
  - added new runtime resource/state:
    - `GameSequencerTrackDispatchState`
    - `GameSequencerTrackDispatch`
    - `GameSequencerDispatchEvent`
  - expanded `GameSequencerRuntimeState` with resolved action context + active track IDs (`active_agent`, `active_action`, `active_track_ids`).
  - added `derive_runtime_track_dispatch_state(...)` consuming:
    - `GameDataRegistryState`
    - `GameSequencerRuntimeState`
    - optional `GameRuntimeActionBindings` + `GameCameraRuntimeState`
    - previous dispatch state for deterministic `frame_index` continuity
  - added parser for optional `[runtime_dispatch]` and `[[runtime_dispatch.routes]]` with deterministic ranking (`priority`, then longest `track_prefix`).
  - added bounded dispatch evaluation:
    - per-frame active track cap (`max_tracks_per_frame`)
    - per-frame event cap (`max_events_per_frame`)
    - event emission from `event.*` tracks with authored/fallback `event_id`
  - added unit tests:
    - route-driven dispatch/event projection
    - fallback dispatch behavior when `[runtime_dispatch]` is absent
- `crates/k-os-bevy`
  - wired `GameSequencerTrackDispatchState` resource in normal + standalone startup paths
  - added `reconcile_game_pipeline_sequencer_dispatch_state` runtime system
  - updated runtime ordering so sequencer dispatch runs after sequencer reconciliation and before input reconciliation
  - extended runtime diagnostics (`Game Runtime` panel) with sequencer dispatch status (`tracks`, `events`, `routes`)
- `sources/game` data/config
  - expanded `sources/game/assets/gameplay/sequencer.registry.toml` with authored `[runtime_dispatch]` routes for camera, audio, and gameplay-event lanes
  - tightened strict schema policy in `sources/game/config/pipeline.toml` so `sequencer_timelines.required_tables` includes `runtime_dispatch`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with sequencer dispatch contract/runtime behavior
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` for sequencer dispatch ownership scope

## Verification

- `cargo test -p k-os-game-sequencer`
- `cargo check -p k-os-bevy`

## Why this matters

Sequencer runtime ownership now produces deterministic, data-driven track/event outputs instead of only selecting a timeline, which creates a stronger bridge into gameplay, camera, audio, and narrative execution owners.

## New constraints

- `sequencer_timelines` registry now requires `[runtime_dispatch]` under strict schema validation.
- Dispatch currently emits runtime state + diagnostics and should be connected next to concrete gameplay/audio/camera consumers for full end-to-end execution.


## Run 30 Summary

Added a gameplay-owned execution transport sink lane that converts deterministic execution effects into a replay/rollback journal with data-driven channel-to-transport mapping.

## Run 30 Added

- `crates/k-os-game-play`
  - added new runtime resource/state:
    - `GameExecutionTransportState`
    - `GameExecutionTransportEnvelope`
  - added `derive_execution_transport_state(...)` consuming:
    - `GameExecutionEffectsState`
    - `GameDataRegistryState` (`input_bindings` snapshot)
    - previous transport state for deterministic journal continuity
  - added parser for optional `[runtime_execution_transport]` contract:
    - `max_journal_events`
    - `rollback_window_frames`
    - `default_lane`
    - `default_reliability`
    - `default_rollback`
    - `[[runtime_execution_transport.channel_routes]]` with deterministic ranking (`priority`, then longest `channel_prefix`)
  - added deterministic transport-journal behavior:
    - monotonic effect-sequence ingestion (dedupes already-seen sequences)
    - bounded journal retention (`max_journal_events`)
    - rollback floor sequence projection using frame window
    - deterministic replay fingerprint over retained journal payload
  - added tests:
    - channel mapping + rollback window behavior
    - dedupe + journal clamp behavior
- `crates/k-os-bevy`
  - added `GameExecutionTransportState` resource wiring in normal + standalone boot paths
  - added `reconcile_game_pipeline_execution_transport` system
  - updated runtime ordering:
    - execution transport now reconciles after execution effects and before host transform apply
  - extended runtime diagnostics panel with execution transport metrics (`ingested`, `retained`, `rollback_floor`, `replay_fingerprint`)
- `sources/game` data/contracts
  - `sources/game/assets/gameplay/input.registry.toml` now includes authored `[runtime_execution_transport]` with channel routes for `unreliable`, `reliable_ordered`, and `reliable`
  - `sources/game/config/pipeline.toml` strict `input_bindings` schema now requires table `runtime_execution_transport`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with execution transport contract/behavior
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to include transport sink ownership in `k-os-game-play`

## Verification

- `cargo test -p k-os-game-play` passed (11/11).
- `cargo check -p k-os-bevy` passed.

## Why this matters

The gameplay pipeline now has a deterministic, data-driven sink between execution effects and runtime transport semantics (lane + reliability + rollback metadata), which is the required foundation for real multiplayer replication backends and replay/rollback adapters.

## New constraints

- `input_bindings` registry now requires `[runtime_execution_transport]` under strict schema policy.
- Transport lane currently owns journal projection/diagnostics only; concrete network backend publishers remain the next increment.


## Run 31 Summary

Bridged sequencer dispatch into concrete camera ownership with a data-driven camera-dispatch projection lane so cinematic tracks can deterministically drive runtime camera rig state.

## Run 31 Added

- `crates/k-os-game-camera`
  - added new runtime resource/state:
    - `GameCameraDispatchState`
    - `GameSequencerCameraDispatchInput`
  - added `derive_runtime_camera_dispatch_state(...)` consuming:
    - `GameDataRegistryState` (`camera_rigs` snapshot)
    - projected sequencer dispatch inputs (binding target + track + intensity)
    - previous dispatch state for deterministic fallback continuity
  - added parser for required `[sequencer_projection]` contract:
    - `[[sequencer_projection.routes]]` with deterministic ranking (`priority`, then longest `binding_target_prefix`, then highest `intensity`)
    - optional route fields: `blend_seconds`, `min_intensity`, `priority`
  - extended `derive_runtime_camera_state(...)` to consume `GameCameraDispatchState` as highest-priority camera source before gameplay-action routing
  - added tests:
    - sequencer projection route drives cinematic camera rig selection
    - previous dispatch-rig fallback when no sequencer route matches
- `crates/k-os-bevy`
  - wired `GameCameraDispatchState` resource in normal + standalone startup paths
  - added `reconcile_game_pipeline_camera_dispatch_state` runtime system
  - updated runtime ordering:
    - `reconcile_game_pipeline_sequencer_state`
    - `reconcile_game_pipeline_sequencer_dispatch_state`
    - `reconcile_game_pipeline_camera_dispatch_state`
    - `reconcile_game_pipeline_camera_state`
    - `reconcile_game_pipeline_input_state`
  - extended runtime diagnostics surfaces with camera-dispatch status (`rig`, `routes`, `track`)
- `sources/game` data/contracts
  - expanded `sources/game/assets/gameplay/cameras.registry.toml` with authored `[sequencer_projection]` routes for cinematic and fallback track bindings
  - tightened strict schema policy in `sources/game/config/pipeline.toml` so `camera_rigs.required_tables` includes `sequencer_projection`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with camera dispatch contract/runtime behavior
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to include sequencer-dispatch camera ownership in `k-os-game-camera`

## Verification

- `cargo test -p k-os-game-camera`
- `cargo check -p k-os-bevy`

## Why this matters

Sequencer dispatch is now consumed by a concrete owner and converted into deterministic camera mutations, closing the gap between dispatch diagnostics and real runtime camera behavior.

## New constraints

- `camera_rigs` registry now requires `[sequencer_projection]` under strict schema policy.
- Sequencer-to-camera projection currently resolves rig/blend ownership; richer camera mode stacks/rails/effect lanes remain the next increment.


## Run 32 Summary

Added a concrete camera-application lane so sequencer/gameplay-selected rigs now produce deterministic runtime camera transforms, not only projection state.

## Run 32 Added

- `crates/k-os-game-camera`
  - added new runtime resource/state:
    - `GameCameraViewRuntimeState`
  - added `derive_runtime_camera_view_state(...)` consuming:
    - `GameDataRegistryState` (`camera_rigs` snapshot)
    - `GameCameraRuntimeState` (active rig + transition)
    - optional `GameCameraDispatchState` (sequencer intensity/blend hints)
  - added data-driven camera-view policy parser for required `[runtime_view]` table:
    - `apply_to_host_camera`
    - `fallback_focus`
    - `fallback_arm_length`
    - spring/rail/freefly solve tuning + dispatch-intensity scales
  - added active-rig runtime spec projection (`target_offset`, `arm_length`, `default_path`, `move_speed`) from `[[rigs]]`
  - added deterministic view solve per mode (`spring_arm`, `rail`, `freefly`) with stable hash-seeded rail orientation
  - added tests:
    - spring-arm view projection from runtime policy
    - rail view projection with dispatch-intensity scaling
- `crates/k-os-bevy`
  - wired `GameCameraViewRuntimeState` in normal + standalone startup paths
  - added `reconcile_game_pipeline_camera_view_state`
  - added `apply_game_pipeline_camera_view_state` to mutate `MainCamera` + `PanOrbitCamera` from owner-projected view state
  - updated ordering so input reconciliation runs after camera view application
- `sources/game` data/contracts
  - expanded `sources/game/assets/gameplay/cameras.registry.toml` with authored `[runtime_view]` policy data
  - tightened strict schema policy in `sources/game/config/pipeline.toml` so `camera_rigs.required_tables` includes `runtime_view`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with camera-view policy + host application lane
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to include `GameCameraViewRuntimeState` ownership

## Verification

- `cargo test -p k-os-game-camera` passed (8/8).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Sequencer dispatch and gameplay camera routing now flow into an actual runtime camera transform/orbit application path, closing the gap between projected rig selection and visible viewport behavior.

## New constraints

- `camera_rigs` registry now requires `[runtime_view]` under strict schema policy.
- Host camera is only mutated by this lane when `runtime_view.apply_to_host_camera = true`, preserving opt-in control for future editor/runtime profiles.


## Run 33 Summary

Added a gameplay-owned transport-backend adapter lane that projects transport journal events into deterministic lane-specific backend payload batches with export-cursor continuity.

## Run 33 Added

- `crates/k-os-game-play`
  - added new runtime resource/state:
    - `GameExecutionTransportBackendState`
    - `GameExecutionTransportBackendPayload`
  - added `derive_execution_transport_backend_state(...)` consuming:
    - `GameExecutionTransportState`
    - `GameDataRegistryState` (`input_bindings` snapshot)
    - previous backend state for deterministic export cursor continuity
  - added parser for optional `[runtime_execution_transport_backends]` contract:
    - `max_payloads_per_frame`
    - `default_backend`
    - `default_payload_kind`
    - `default_max_events_per_payload`
    - `[[runtime_execution_transport_backends.lane_backends]]` with deterministic ranking (`priority`, then longest `lane_prefix`)
  - added deterministic backend payload behavior:
    - per-lane backend route selection for `transport_lane` values
    - bounded payload chunking by `max_events_per_payload`
    - bounded per-frame payload emission by `max_payloads_per_frame`
    - monotonic `exported_through_sequence` progression
    - deterministic backend fingerprint over emitted payloads
  - added tests:
    - lane-route backend mapping + payload chunk behavior
    - export-cursor resume + payload clamp behavior
- `crates/k-os-bevy`
  - added `GameExecutionTransportBackendState` resource wiring in normal + standalone boot paths
  - added `reconcile_game_pipeline_execution_transport_backends` system
  - updated runtime ordering:
    - execution transport backends now reconcile after execution transport and before host transform apply
  - extended runtime diagnostics panel with execution transport backend metrics (`payloads`, `lanes`, `exported_through`, `rollback_floor`, `backend_fingerprint`)
- `sources/game` data/contracts
  - `sources/game/assets/gameplay/input.registry.toml` now includes authored `[runtime_execution_transport_backends]` + lane backend routes for `state_delta`, `gameplay_rpc`, and `rollback_journal`
  - `sources/game/config/pipeline.toml` strict `input_bindings` schema now requires table `runtime_execution_transport_backends`
- docs
  - updated `sources/game/docs/PIPELINE_ARCHITECTURE.md` with backend adapter policy/behavior
  - updated `sources/game/docs/CRATE_ARCHITECTURE.md` to include transport-backend payload ownership in `k-os-game-play`

## Verification

- `cargo test -p k-os-game-play` passed (13/13).
- `cargo check -p k-os-bevy` passed.

## Why this matters

Execution transport no longer stops at journal retention. The gameplay layer now emits deterministic backend payload contracts that can be consumed directly by host or multiplayer transport adapters without embedding lane logic in `k-os-bevy`.

## New constraints

- `input_bindings` registry now requires `[runtime_execution_transport_backends]` under strict schema policy.
- Backend payload projection is currently diagnostics/contract owned; concrete runtime socket/bus publishers are the next integration step.
