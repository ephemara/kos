[package]
name = "k-os-game-runtime"
version = "0.1.0"
edition = "2021"
description = "Data-driven K_OS game runtime pipeline manifest and planning"

[lib]
name = "k_os_game_runtime"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
thiserror = "2.0"
toml = "0.8"
