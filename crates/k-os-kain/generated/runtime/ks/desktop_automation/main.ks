// @ts-check
// Generated by KAIN compiler — KainScript target (.ks)
// Runs natively in Node.js, Deno, Bun, and browsers. No compilation needed.
// Full type checking: tsc --checkJs --noEmit file.ks
/* eslint-disable */

// ── Numeric type coercion helpers ─────────────────────────────────────────
/** @param {number} n @returns {number} */ function u8(n)  { return n & 0xFF; }
/** @param {number} n @returns {number} */ function u16(n) { return n & 0xFFFF; }
/** @param {number} n @returns {number} */ function u32(n) { return n >>> 0; }
/** @param {number} n @returns {number} */ function i8(n)  { return (n << 24) >> 24; }
/** @param {number} n @returns {number} */ function i16(n) { return (n << 16) >> 16; }
/** @param {number} n @returns {number} */ function i32(n) { return n | 0; }
/** @param {number} n @returns {number} */ function f32(n) { return Math.fround(n); }

// ── KAIN stdlib bridge (auto-generated) ───────────────────────────────────
function __kain_clamp(v, lo, hi) { return v < lo ? lo : v > hi ? hi : v; }
function println(...a) { console.log(...a); }
function print(...a) { process?.stdout?.write(String(a[0])) ?? console.log(...a); }
function push(arr, v) { arr.push(v); return arr; }
function pop(arr) { return arr.pop(); }
function len(v) { return v?.length ?? 0; }
function is_empty(v) { return (v?.length ?? 0) === 0; }
function map(arr, f) { return arr.map(f); }
function filter(arr, f) { return arr.filter(f); }
function reduce(arr, f, init) { return arr.reduce(f, init); }
function to_string(v) { return String(v); }
function parse_int(s) { return parseInt(s, 10); }
function parse_float(s) { return parseFloat(s); }
function http_get(url) { return fetch(url).then(r => r.text()); }
function http_post(url, body) { return fetch(url, { method: 'POST', body: JSON.stringify(body) }).then(r => r.text()); }
function json_parse(s) { return JSON.parse(s); }
function json_stringify(v) { return JSON.stringify(v); }

/**
 * @kain-effects Pure
 * @returns {string}
 */
function desktop_automation_name() {
  "desktop_automation";
}
