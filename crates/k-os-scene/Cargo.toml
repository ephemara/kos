[package]
name = "k-os-scene"
version = "0.1.0"
edition = "2021"
description = "K_OS canonical scene data layer backed by bevy_ecs"

[lib]
name = "k_os_scene"
path = "src/lib.rs"

[dependencies]
bevy_ecs = "0.17"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
