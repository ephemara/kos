[package]
name = "zen-host"
version = "0.1.0"
edition = "2021"
description = "Zen native host widget/action registry and manifest loader"

[dependencies]
k-os-kain = { path = "../k-os-kain" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
zen-core = { path = "../zen-core" }
