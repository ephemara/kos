[package]
name = "k-os-bevy"
version = "0.1.0"
description = "K_OS Bevy Renderer - High-Performance 3D Viewport"
authors = ["K_OS Team"]
edition = "2021"

[[bin]]
name = "k-os-bevy"
path = "src/main.rs"

[dependencies]
# === K_OS ENGINE (All heavy compute lives here) ===
k-os-brushes = { path = "../k-os-brushes" }
k-os-gpu-pipeline = { path = "../k-os-gpu-pipeline" }
k-os-workspace-registry = { path = "../k-os-workspace-registry" }
k-os-mesh = { path = "../k-os-mesh" }
k-os-sculpt = { path = "../k-os-sculpt" }
k-os-game-runtime = { path = "../k-os-game-runtime" }
k-os-game-framework = { path = "../k-os-game-framework" }
k-os-game-play = { path = "../k-os-game-play" }
k-os-game-ai = { path = "../k-os-game-ai" }
k-os-game-camera = { path = "../k-os-game-camera" }
k-os-game-input = { path = "../k-os-game-input" }
k-os-game-sequencer = { path = "../k-os-game-sequencer" }

# === BEVY CORE ===
bevy = { version = "0.17", default-features = true, features = ["file_watcher"] }
bevy_panorbit_camera = { version = "0.33", features = ["bevy_egui"] }
bevy_egui = "0.38"
bevy_hanabi = "0.17"
bevy_tweening = "0.14"
bevy-inspector-egui = "0.35"

# === SHARED DEPS ===
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
log = "0.4"
env_logger = "0.11"
glam = { version = "0.30", features = ["serde", "bytemuck"] }
bytemuck = { version = "1.20", features = ["derive"] }
wgpu = "26"
base64 = "0.22"
rand = "0.8"
image = "0.25"

# Note: Profile settings are inherited from workspace root Cargo.toml
