[package]
name = "zen-render"
version = "0.1.0"
edition = "2021"
description = "Zen renderer profile and frame graph metadata contracts"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
