// Generated by KAIN Compiler (Project Ouroboros)
// Do not edit - regenerate from .kn source

#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_parens)]

use std::collections::HashMap;
use std::rc::Rc;
use std::cell::RefCell;
use std::cmp::min;
use std::mem::size_of;

fn __kain_union_wrap<TObject: Copy, TValue: Copy>(mut value: TObject, _active: &str, _type_key: &str, byte_size: i64, union_size: i64, active_value: TValue) -> TObject {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&active_value as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    value
}
fn __kain_union_get<TObject: Copy, TValue: Copy>(value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, fallback: TValue) -> TValue {
    let mut result = fallback;
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe { if copy_span > 0 { std::ptr::copy_nonoverlapping((&value as *const TObject).cast::<u8>(), (&mut result as *mut TValue).cast::<u8>(), copy_span); } }
    result
}
fn __kain_union_set<TObject: Copy, TValue: Copy>(mut value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, next: TValue) -> TValue {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&next as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    next
}
fn __kain_load_bitfield_unit<T: Copy>(value: &T, unit_offset: i64) -> u64 {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return 0; }
    let mut unit = 0u64;
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((value as *const T).cast::<u8>().add(unit_offset as usize), (&mut unit as *mut u64).cast::<u8>(), available); }
    unit
}
fn __kain_store_bitfield_unit<T: Copy>(value: &mut T, unit_offset: i64, unit: u64) {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return; }
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((&unit as *const u64).cast::<u8>(), (value as *mut T).cast::<u8>().add(unit_offset as usize), available); }
}
fn __kain_bitfield_mask(width: i64) -> u64 { if width <= 0 { 0 } else if width >= 64 { u64::MAX } else { (1u64 << width) - 1 } }
fn __kain_sign_extend(value: u64, width: i64) -> i64 { if width <= 0 { 0 } else if width >= 64 { value as i64 } else { let sign_bit = 1u64 << (width - 1); if (value & sign_bit) == 0 { value as i64 } else { (value | !__kain_bitfield_mask(width)) as i64 } } }
fn __kain_bitfield_get<T: Copy>(value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, _promoted_bits: i64) -> i64 {
    let mask = __kain_bitfield_mask(width);
    let unit = __kain_load_bitfield_unit(&value, unit_offset);
    let shifted = if bit_offset <= 0 { unit } else { unit >> bit_offset };
    let encoded = shifted & mask;
    if is_signed { __kain_sign_extend(encoded, width) } else { encoded as i64 }
}
fn __kain_bitfield_set<T: Copy, TValue: Copy + Into<i64>>(mut value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, promoted_bits: i64, next: TValue) -> TValue {
    let mask = __kain_bitfield_mask(width);
    let mut unit = __kain_load_bitfield_unit(&value, unit_offset);
    let encoded = (next.into() as u64) & mask;
    let shifted_mask = if bit_offset <= 0 { mask } else { mask << bit_offset };
    unit = (unit & !shifted_mask) | if bit_offset <= 0 { encoded } else { encoded << bit_offset };
    __kain_store_bitfield_unit(&mut value, unit_offset, unit);
    let _ = __kain_bitfield_get(value, "", unit_offset, bit_offset, width, is_signed, promoted_bits);
    next
}

#[derive(Debug, Clone)]
struct kiss_fft_cpx {
    pub r: f64,
    pub i: f64,
}

#[derive(Debug, Clone)]
struct kiss_fft_state {
}

#[derive(Debug, Clone)]
struct kiss_fftr_state {
}

#[derive(Debug, Clone)]
struct kiss_fft_state {
    pub nfft: i64,
    pub inverse: i64,
    pub factors: [i64],
    pub twiddles: [kiss_fft_cpx; 1],
}

#[derive(Debug, Clone)]
struct kiss_fftr_state {
    pub substate: &mut kiss_fft_state,
    pub tmpbuf: &mut kiss_fft_cpx,
    pub super_twiddles: &mut kiss_fft_cpx,
}

fn kiss_fftr_alloc(nfft: i64, inverse_fft: i64, mem: &mut (), lenmem: &mut i64) -> &mut kiss_fftr_state {
    let mut i: i64 = None;
    let mut st: &mut kiss_fftr_state = (0 as &mut ());
    let mut subsize: i64 = 0;
    let mut __kain_ptr_subsize: i64 = __kain_bind_local(subsize);
    let mut memneeded: i64 = None;
    if ((nfft as i64 & 1 as i64)) { fprintf((0 as &mut ()), "[ERROR] M:\\Code\\Other\\cimport\\signalprocessing\\kiss_fft\\kiss_fftr.c:30 ".to_string()); fprintf((0 as &mut ()), "Real FFT optimization must be even.".to_string()); fprintf((0 as &mut ()), "\n".to_string()); return (0 as &mut ()) }(nfft: ((nfft as i64 >> 1 as i64)));
    kiss_fft_alloc(nfft, inverse_fft, (0 as &mut ()), __kain_ptr_subsize);
    (/* unhandled expr */);
    if ((lenmem == (0 as &mut ()))) { (/* unhandled expr */) } else { if ((__kain_mem_load(lenmem) >= memneeded)) { (/* unhandled expr */) }; __kain_mem_store(lenmem, memneeded) };
    if ((!st)) { return (0 as &mut ()) }(/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    kiss_fft_alloc(nfft, inverse_fft, st.substate, __kain_ptr_subsize);
    (/* unhandled expr */);
    while ((i < ((nfft as i64 / 2 as i64)))) {
        let mut phase: f64 = ((((-3.1 as f64)) * (((((((i as i64 + 1 as i64)) as f64) / nfft)) + 0.5))));
        if inverse_fft { (/* unhandled expr */) }(i: ((i as i64 + 1 as i64)));
    }
    return st;
}

fn kiss_fftr(st: &mut kiss_fftr_state, timedata: &mut f64, freqdata: &mut kiss_fft_cpx) -> () {
    let mut k: i64 = None;
    let mut ncfft: i64 = None;
    let mut fpnk: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    let mut fpk: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    let mut f1k: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    let mut f2k: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    let mut tw: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    let mut tdc: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
    if st.substate.inverse { fprintf((0 as &mut ()), "[ERROR] M:\\Code\\Other\\cimport\\signalprocessing\\kiss_fft\\kiss_fftr.c:70 ".to_string()); fprintf((0 as &mut ()), "kiss fft usage error: improper alloc".to_string()); fprintf((0 as &mut ()), "\n".to_string()); return }(ncfft: st.substate.nfft);
    kiss_fft(st.substate, (timedata as &mut kiss_fft_cpx), st.tmpbuf);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    while ((k <= ((ncfft as i64 / 2 as i64)))) {
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn kiss_fftri(st: &mut kiss_fftr_state, freqdata: &mut kiss_fft_cpx, timedata: &mut f64) -> () {
    let mut k: i64 = None;
    let mut ncfft: i64 = None;
    if ((st.substate.inverse as i64 == 0 as i64)) { fprintf((0 as &mut ()), "[ERROR] M:\\Code\\Other\\cimport\\signalprocessing\\kiss_fft\\kiss_fftr.c:125 ".to_string()); fprintf((0 as &mut ()), "kiss fft usage error: improper alloc".to_string()); fprintf((0 as &mut ()), "\n".to_string()); return }(ncfft: st.substate.nfft);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    while ((k <= ((ncfft as i64 / 2 as i64)))) {
        let mut fk: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
        let mut fnkc: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
        let mut fek: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
        let mut fok: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
        let mut tmp: kiss_fft_cpx = kiss_fft_cpx { r: None, i: None };
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    kiss_fft(st.substate, st.tmpbuf, (timedata as &mut kiss_fft_cpx));
}

