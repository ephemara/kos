// Generated by KAIN Compiler (Project Ouroboros)
// Do not edit - regenerate from .kn source

#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(unused_parens)]

use std::collections::HashMap;
use std::rc::Rc;
use std::cell::RefCell;
use std::cmp::min;
use std::mem::size_of;

fn __kain_union_wrap<TObject: Copy, TValue: Copy>(mut value: TObject, _active: &str, _type_key: &str, byte_size: i64, union_size: i64, active_value: TValue) -> TObject {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&active_value as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    value
}
fn __kain_union_get<TObject: Copy, TValue: Copy>(value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, fallback: TValue) -> TValue {
    let mut result = fallback;
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe { if copy_span > 0 { std::ptr::copy_nonoverlapping((&value as *const TObject).cast::<u8>(), (&mut result as *mut TValue).cast::<u8>(), copy_span); } }
    result
}
fn __kain_union_set<TObject: Copy, TValue: Copy>(mut value: TObject, _field: &str, _type_key: &str, byte_size: i64, union_size: i64, next: TValue) -> TValue {
    let zero_span = min(union_size.max(0) as usize, size_of::<TObject>());
    let copy_span = min(min(byte_size.max(0) as usize, union_size.max(0) as usize), min(size_of::<TObject>(), size_of::<TValue>()));
    unsafe {
        std::ptr::write_bytes((&mut value as *mut TObject).cast::<u8>(), 0, zero_span);
        if copy_span > 0 { std::ptr::copy_nonoverlapping((&next as *const TValue).cast::<u8>(), (&mut value as *mut TObject).cast::<u8>(), copy_span); }
    }
    next
}
fn __kain_load_bitfield_unit<T: Copy>(value: &T, unit_offset: i64) -> u64 {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return 0; }
    let mut unit = 0u64;
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((value as *const T).cast::<u8>().add(unit_offset as usize), (&mut unit as *mut u64).cast::<u8>(), available); }
    unit
}
fn __kain_store_bitfield_unit<T: Copy>(value: &mut T, unit_offset: i64, unit: u64) {
    if unit_offset < 0 || unit_offset as usize >= size_of::<T>() { return; }
    let available = min(8usize, size_of::<T>() - unit_offset as usize);
    unsafe { std::ptr::copy_nonoverlapping((&unit as *const u64).cast::<u8>(), (value as *mut T).cast::<u8>().add(unit_offset as usize), available); }
}
fn __kain_bitfield_mask(width: i64) -> u64 { if width <= 0 { 0 } else if width >= 64 { u64::MAX } else { (1u64 << width) - 1 } }
fn __kain_sign_extend(value: u64, width: i64) -> i64 { if width <= 0 { 0 } else if width >= 64 { value as i64 } else { let sign_bit = 1u64 << (width - 1); if (value & sign_bit) == 0 { value as i64 } else { (value | !__kain_bitfield_mask(width)) as i64 } } }
fn __kain_bitfield_get<T: Copy>(value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, _promoted_bits: i64) -> i64 {
    let mask = __kain_bitfield_mask(width);
    let unit = __kain_load_bitfield_unit(&value, unit_offset);
    let shifted = if bit_offset <= 0 { unit } else { unit >> bit_offset };
    let encoded = shifted & mask;
    if is_signed { __kain_sign_extend(encoded, width) } else { encoded as i64 }
}
fn __kain_bitfield_set<T: Copy, TValue: Copy + Into<i64>>(mut value: T, _field: &str, unit_offset: i64, bit_offset: i64, width: i64, is_signed: bool, promoted_bits: i64, next: TValue) -> TValue {
    let mask = __kain_bitfield_mask(width);
    let mut unit = __kain_load_bitfield_unit(&value, unit_offset);
    let encoded = (next.into() as u64) & mask;
    let shifted_mask = if bit_offset <= 0 { mask } else { mask << bit_offset };
    unit = (unit & !shifted_mask) | if bit_offset <= 0 { encoded } else { encoded << bit_offset };
    __kain_store_bitfield_unit(&mut value, unit_offset, unit);
    let _ = __kain_bitfield_get(value, "", unit_offset, bit_offset, width, is_signed, promoted_bits);
    next
}

fn vec2_add(r: [f64; 2], a: [f64; 2], b: [f64; 2]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec2_sub(r: [f64; 2], a: [f64; 2], b: [f64; 2]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec2_scale(r: [f64; 2], v: [f64; 2], s: f64) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec2_mul_inner(a: [f64; 2], b: [f64; 2]) -> f64 {
    let mut p: f64 = 0.0;
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    return p;
}

fn vec2_len(v: [f64; 2]) -> f64 {
    return sqrtf(vec2_mul_inner(v, v));
}

fn vec2_norm(r: [f64; 2], v: [f64; 2]) -> () {
    let mut k: f64 = ((1.0 / vec2_len(v)));
    vec2_scale(r, v, k);
}

fn vec2_min(r: [f64; 2], a: [f64; 2], b: [f64; 2]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec2_max(r: [f64; 2], a: [f64; 2], b: [f64; 2]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec2_dup(r: [f64; 2], src: [f64; 2]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 2 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_add(r: [f64; 3], a: [f64; 3], b: [f64; 3]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_sub(r: [f64; 3], a: [f64; 3], b: [f64; 3]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_scale(r: [f64; 3], v: [f64; 3], s: f64) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_mul_inner(a: [f64; 3], b: [f64; 3]) -> f64 {
    let mut p: f64 = 0.0;
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    return p;
}

fn vec3_len(v: [f64; 3]) -> f64 {
    return sqrtf(vec3_mul_inner(v, v));
}

fn vec3_norm(r: [f64; 3], v: [f64; 3]) -> () {
    let mut k: f64 = ((1.0 / vec3_len(v)));
    vec3_scale(r, v, k);
}

fn vec3_min(r: [f64; 3], a: [f64; 3], b: [f64; 3]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_max(r: [f64; 3], a: [f64; 3], b: [f64; 3]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_dup(r: [f64; 3], src: [f64; 3]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_add(r: [f64; 4], a: [f64; 4], b: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_sub(r: [f64; 4], a: [f64; 4], b: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_scale(r: [f64; 4], v: [f64; 4], s: f64) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_mul_inner(a: [f64; 4], b: [f64; 4]) -> f64 {
    let mut p: f64 = 0.0;
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    return p;
}

fn vec4_len(v: [f64; 4]) -> f64 {
    return sqrtf(vec4_mul_inner(v, v));
}

fn vec4_norm(r: [f64; 4], v: [f64; 4]) -> () {
    let mut k: f64 = ((1.0 / vec4_len(v)));
    vec4_scale(r, v, k);
}

fn vec4_min(r: [f64; 4], a: [f64; 4], b: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_max(r: [f64; 4], a: [f64; 4], b: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_dup(r: [f64; 4], src: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec3_mul_cross(r: [f64; 3], a: [f64; 3], b: [f64; 3]) -> () {
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn vec3_reflect(r: [f64; 3], v: [f64; 3], n: [f64; 3]) -> () {
    let mut p: f64 = ((2.0 * vec3_mul_inner(v, n)));
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn vec4_mul_cross(r: [f64; 4], a: [f64; 4], b: [f64; 4]) -> () {
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn vec4_reflect(r: [f64; 4], v: [f64; 4], n: [f64; 4]) -> () {
    let mut p: f64 = ((2.0 * vec4_mul_inner(v, n)));
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn mat4x4_identity(M: [[f64; 4]; 4]) -> () {
    let mut i: i64 = None;
    let mut j: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        while ((j as i64 < 4 as i64)) {
            (/* unhandled expr */);
            (/* unhandled expr */);
        }
        (/* unhandled expr */);
    }
}

fn mat4x4_dup(M: [[f64; 4]; 4], N: [[f64; 4]; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        vec4_dup(M[i], N[i]);
        (/* unhandled expr */);
    }
}

fn mat4x4_row(r: [f64; 4], M: [[f64; 4]; 4], i: i64) -> () {
    let mut k: i64 = None;
    (/* unhandled expr */);
    while ((k as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn mat4x4_col(r: [f64; 4], M: [[f64; 4]; 4], i: i64) -> () {
    let mut k: i64 = None;
    (/* unhandled expr */);
    while ((k as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn mat4x4_transpose(M: [[f64; 4]; 4], N: [[f64; 4]; 4]) -> () {
    let mut i: i64 = None;
    let mut j: i64 = None;
    (/* unhandled expr */);
    while ((j as i64 < 4 as i64)) {
        (/* unhandled expr */);
        while ((i as i64 < 4 as i64)) {
            (/* unhandled expr */);
            (/* unhandled expr */);
        }
        (/* unhandled expr */);
    }
}

fn mat4x4_add(M: [[f64; 4]; 4], a: [[f64; 4]; 4], b: [[f64; 4]; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        vec4_add(M[i], a[i], b[i]);
        (/* unhandled expr */);
    }
}

fn mat4x4_sub(M: [[f64; 4]; 4], a: [[f64; 4]; 4], b: [[f64; 4]; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        vec4_sub(M[i], a[i], b[i]);
        (/* unhandled expr */);
    }
}

fn mat4x4_scale(M: [[f64; 4]; 4], a: [[f64; 4]; 4], k: f64) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        vec4_scale(M[i], a[i], k);
        (/* unhandled expr */);
    }
}

fn mat4x4_scale_aniso(M: [[f64; 4]; 4], a: [[f64; 4]; 4], x: f64, y: f64, z: f64) -> () {
    vec4_scale(M[0], a[0], x);
    vec4_scale(M[1], a[1], y);
    vec4_scale(M[2], a[2], z);
    vec4_dup(M[3], a[3]);
}

fn mat4x4_mul(M: [[f64; 4]; 4], a: [[f64; 4]; 4], b: [[f64; 4]; 4]) -> () {
    let mut temp: [[f64; 4]; 4] = vec![vec![None, None, None, None], vec![None, None, None, None], vec![None, None, None, None], vec![None, None, None, None]];
    let mut k: i64 = None;
    let mut r: i64 = None;
    let mut c: i64 = None;
    (/* unhandled expr */);
    while ((c as i64 < 4 as i64)) {
        (/* unhandled expr */);
        while ((r as i64 < 4 as i64)) {
            (/* unhandled expr */);
            (/* unhandled expr */);
            while ((k as i64 < 4 as i64)) {
                (/* unhandled expr */);
                (/* unhandled expr */);
            }
            (/* unhandled expr */);
        }
        (/* unhandled expr */);
    }
    mat4x4_dup(M, temp);
}

fn mat4x4_mul_vec4(r: [f64; 4], M: [[f64; 4]; 4], v: [f64; 4]) -> () {
    let mut i: i64 = None;
    let mut j: i64 = None;
    (/* unhandled expr */);
    while ((j as i64 < 4 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
        while ((i as i64 < 4 as i64)) {
            (/* unhandled expr */);
            (/* unhandled expr */);
        }
        (/* unhandled expr */);
    }
}

fn mat4x4_translate(T: [[f64; 4]; 4], x: f64, y: f64, z: f64) -> () {
    mat4x4_identity(T);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_translate_in_place(M: [[f64; 4]; 4], x: f64, y: f64, z: f64) -> () {
    let mut t: [f64; 4] = vec![x, y, z, 0];
    let mut r: [f64; 4] = vec![None, None, None, None];
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        mat4x4_row(r, M, i);
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
}

fn mat4x4_from_vec3_mul_outer(M: [[f64; 4]; 4], a: [f64; 3], b: [f64; 3]) -> () {
    let mut i: i64 = None;
    let mut j: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 4 as i64)) {
        (/* unhandled expr */);
        while ((j as i64 < 4 as i64)) {
            (/* unhandled expr */);
            (/* unhandled expr */);
        }
        (/* unhandled expr */);
    }
}

fn mat4x4_rotate(R: [[f64; 4]; 4], M: [[f64; 4]; 4], x: f64, y: f64, z: f64, angle: f64) -> () {
    let mut s: f64 = sinf(angle);
    let mut c: f64 = cosf(angle);
    let mut u: [f64; 3] = vec![x, y, z];
    if ((vec3_len(u) > 0.0)) { vec3_norm(u, u); mat4x4_from_vec3_mul_outer(T, u, u); mat4x4_scale(S, S, s); mat4x4_identity(C); mat4x4_sub(C, C, T); mat4x4_scale(C, C, c); mat4x4_add(T, T, C); mat4x4_add(T, T, S); (/* unhandled expr */); mat4x4_mul(R, M, T) } else { mat4x4_dup(R, M) };
}

fn mat4x4_rotate_X(Q: [[f64; 4]; 4], M: [[f64; 4]; 4], angle: f64) -> () {
    let mut s: f64 = sinf(angle);
    let mut c: f64 = cosf(angle);
    let mut R: [[f64; 4]; 4] = vec![vec![1.0, 0.0, 0.0, 0.0], vec![0.0, c, s, 0.0], vec![0.0, ((-s as f64)), c, 0.0], vec![0.0, 0.0, 0.0, 1.0]];
    mat4x4_mul(Q, M, R);
}

fn mat4x4_rotate_Y(Q: [[f64; 4]; 4], M: [[f64; 4]; 4], angle: f64) -> () {
    let mut s: f64 = sinf(angle);
    let mut c: f64 = cosf(angle);
    let mut R: [[f64; 4]; 4] = vec![vec![c, 0.0, ((-s as f64)), 0.0], vec![0.0, 1.0, 0.0, 0.0], vec![s, 0.0, c, 0.0], vec![0.0, 0.0, 0.0, 1.0]];
    mat4x4_mul(Q, M, R);
}

fn mat4x4_rotate_Z(Q: [[f64; 4]; 4], M: [[f64; 4]; 4], angle: f64) -> () {
    let mut s: f64 = sinf(angle);
    let mut c: f64 = cosf(angle);
    let mut R: [[f64; 4]; 4] = vec![vec![c, s, 0.0, 0.0], vec![((-s as f64)), c, 0.0, 0.0], vec![0.0, 0.0, 1.0, 0.0], vec![0.0, 0.0, 0.0, 1.0]];
    mat4x4_mul(Q, M, R);
}

fn mat4x4_invert(T: [[f64; 4]; 4], M: [[f64; 4]; 4]) -> () {
    let mut s: [f64; 6] = vec![None, None, None, None, None, None];
    let mut c: [f64; 6] = vec![None, None, None, None, None, None];
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    let mut idet: f64 = ((1.0 / ((((((((((((s[0] as f64 * c[5] as f64)) - ((s[1] as f64 * c[4] as f64)))) + ((s[2] as f64 * c[3] as f64)))) + ((s[3] as f64 * c[2] as f64)))) - ((s[4] as f64 * c[1] as f64)))) + ((s[5] as f64 * c[0] as f64))))));
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_orthonormalize(R: [[f64; 4]; 4], M: [[f64; 4]; 4]) -> () {
    mat4x4_dup(R, M);
    let mut s: f64 = 1.0;
    let mut h: [f64; 3] = vec![None, None, None];
    vec3_norm(R[2], R[2]);
    (/* unhandled expr */);
    vec3_scale(h, R[2], s);
    vec3_sub(R[1], R[1], h);
    vec3_norm(R[1], R[1]);
    (/* unhandled expr */);
    vec3_scale(h, R[2], s);
    vec3_sub(R[0], R[0], h);
    (/* unhandled expr */);
    vec3_scale(h, R[1], s);
    vec3_sub(R[0], R[0], h);
    vec3_norm(R[0], R[0]);
}

fn mat4x4_frustum(M: [[f64; 4]; 4], l: f64, r: f64, b: f64, t: f64, n: f64, f: f64) -> () {
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_ortho(M: [[f64; 4]; 4], l: f64, r: f64, b: f64, t: f64, n: f64, f: f64) -> () {
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_perspective(m: [[f64; 4]; 4], y_fov: f64, aspect: f64, n: f64, f: f64) -> () {
    let mut a: f64 = ((1.0 / tanf(((y_fov as f64 / 2.0 as f64)))));
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_look_at(m: [[f64; 4]; 4], eye: [f64; 3], center: [f64; 3], up: [f64; 3]) -> () {
    let mut f: [f64; 3] = vec![None, None, None];
    vec3_sub(f, center, eye);
    vec3_norm(f, f);
    let mut s: [f64; 3] = vec![None, None, None];
    vec3_mul_cross(s, f, up);
    vec3_norm(s, s);
    let mut t: [f64; 3] = vec![None, None, None];
    vec3_mul_cross(t, s, f);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    mat4x4_translate_in_place(m, ((-eye[0] as f64)), ((-eye[1] as f64)), ((-eye[2] as f64)));
}

fn quat_identity(q: [f64; 4]) -> () {
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn quat_mul(r: [f64; 4], p: [f64; 4], q: [f64; 4]) -> () {
    let mut w: [f64; 3] = vec![None, None, None];
    let mut tmp: [f64; 3] = vec![None, None, None];
    vec3_mul_cross(tmp, p, q);
    vec3_scale(w, p, q[3]);
    vec3_add(tmp, tmp, w);
    vec3_scale(w, q, p[3]);
    vec3_add(tmp, tmp, w);
    vec3_dup(r, tmp);
    (/* unhandled expr */);
}

fn quat_conj(r: [f64; 4], q: [f64; 4]) -> () {
    let mut i: i64 = None;
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    (/* unhandled expr */);
}

fn quat_rotate(r: [f64; 4], angle: f64, axis: [f64; 3]) -> () {
    let mut axis_norm: [f64; 3] = vec![None, None, None];
    vec3_norm(axis_norm, axis);
    let mut s: f64 = sinf(((angle as f64 / 2 as f64)));
    let mut c: f64 = cosf(((angle as f64 / 2 as f64)));
    vec3_scale(r, axis_norm, s);
    (/* unhandled expr */);
}

fn quat_mul_vec3(r: [f64; 3], q: [f64; 4], v: [f64; 3]) -> () {
    let mut t: [f64; 3] = vec![None, None, None];
    let mut q_xyz: [f64; 3] = vec![q[0], q[1], q[2]];
    let mut u: [f64; 3] = vec![q[0], q[1], q[2]];
    vec3_mul_cross(t, q_xyz, v);
    vec3_scale(t, t, 2);
    vec3_mul_cross(u, q_xyz, t);
    vec3_scale(t, t, q[3]);
    vec3_add(r, v, t);
    vec3_add(r, r, u);
}

fn mat4x4_from_quat(M: [[f64; 4]; 4], q: [f64; 4]) -> () {
    let mut a: f64 = q[3];
    let mut b: f64 = q[0];
    let mut c: f64 = q[1];
    let mut d: f64 = q[2];
    let mut a2: f64 = ((a as f64 * a as f64));
    let mut b2: f64 = ((b as f64 * b as f64));
    let mut c2: f64 = ((c as f64 * c as f64));
    let mut d2: f64 = ((d as f64 * d as f64));
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4o_mul_quat(R: [[f64; 4]; 4], M: [[f64; 4]; 4], q: [f64; 4]) -> () {
    quat_mul_vec3(R[0], q, M[0]);
    quat_mul_vec3(R[1], q, M[1]);
    quat_mul_vec3(R[2], q, M[2]);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn quat_from_mat4x4(q: [f64; 4], M: [[f64; 4]; 4]) -> () {
    let mut r: f64 = 0.0;
    let mut i: i64 = None;
    let mut perm: [i64] = vec![0, 1, 2, 0, 1];
    let mut __kain_ptr_perm: i64 = __kain_bind_local(perm);
    let mut p: &mut i64 = __kain_mem_load(__kain_ptr_perm) as [i64];
    (/* unhandled expr */);
    while ((i as i64 < 3 as i64)) {
        let mut m: f64 = M[i][i];
        if ((m as f64 < r as f64)) {  }(m: r);
        (/* unhandled expr */);
        (/* unhandled expr */);
    }
    (/* unhandled expr */);
    if ((r as f64 < 0.0 as f64)) { (/* unhandled expr */); (/* unhandled expr */); return }(/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
    (/* unhandled expr */);
}

fn mat4x4_arcball(R: [[f64; 4]; 4], M: [[f64; 4]; 4], a_2: [f64; 2], b_2: [f64; 2], s: f64) -> () {
    let mut a: [f64; 2] = vec![None, None];
    memcpy(a, a_2, 16);
    let mut b: [f64; 2] = vec![None, None];
    memcpy(b, b_2, 16);
    let mut z_a: f64 = 0.0;
    let mut z_b: f64 = 0.0;
    if ((vec2_len(a) < 1.0)) { (/* unhandled expr */) } else { vec2_norm(a, a) };
    if ((vec2_len(b) < 1.0)) { (/* unhandled expr */) } else { vec2_norm(b, b) };
    let mut a_3: [f64; 3] = vec![a[0], a[1], z_a];
    let mut b_3: [f64; 3] = vec![b[0], b[1], z_b];
    let mut c_2: [f64; 3] = vec![None, None, None];
    vec3_mul_cross(c_2, a_3, b_3);
    let mut angle: f64 = ((acos(vec3_mul_inner(a_3, b_3)) * s));
    mat4x4_rotate(R, M, c_2[0], c_2[1], c_2[2], angle);
}

