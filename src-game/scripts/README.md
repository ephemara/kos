# src-game scripts

This folder contains Kain gameplay/visual-scripting source files consumed by the `kain_hot_reload` and `visual_scripting_registry` stages.

Planned initial scripts:
- `player_interaction.kain`
- `camera_shake.kain`
- `ai_patrol.kain`
