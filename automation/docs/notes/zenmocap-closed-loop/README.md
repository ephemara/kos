# ZenMocap Closed Loop Notes

This folder is the durable note trail for the ZenMocap automation cycle.

Recommended pattern per run:

- one note file per run
- include run id, turn, phase, summary, evidence, and blockers
- keep enough detail here for the next automation interval to resume without guessing
