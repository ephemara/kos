# Validation outputs

This folder holds targeted import probes and diagnostic artifacts.

Use it for:
- narrowed import experiments
- semantic-loss reports
- comparison outputs between import passes
- repair hints that point at specific lowered seams

The older `_validation/` directory is left intact for now so the raw trail stays visible.
