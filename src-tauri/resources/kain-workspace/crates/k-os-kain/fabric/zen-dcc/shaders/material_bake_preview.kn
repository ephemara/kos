shader compute ZenDccMaterialBakePreview(id: UVec3) -> Vec4:
    uniform src: StorageBuffer<Float> @0
    uniform dst: StorageBuffer<Float> @1
    uniform LOCAL_SIZE_X: UInt @100
    uniform LOCAL_SIZE_Y: UInt @101
    uniform LOCAL_SIZE_Z: UInt @102

    comptime:
        let compute = (
            [8, 1, 1],
            [
                ("src", "f32", ["dispatch.x"], "input", "kain.shared.buffer"),
                ("dst", "f32", ["dispatch.x"], "output", "kain.shared.buffer"),
            ],
            [
                ("zen_dcc_material_bake_preview", "copy_preview_stream", ["src"], ["dst"], false),
            ],
        )

    let index = id.x
    let input_value = src[index]
    dst[index] = input_value
    return vec4(input_value, input_value * 0.82, input_value * 0.64, 1.0)
